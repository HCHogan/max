{"cases":[{"actual_models":["qwen3.8-27b"],"children":[{"report":{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"payload":{"claims":["Child grants are an exact (name+contract) intersection: authority = Map.filterWithKey (name -> Map.lookup name task.grants == Just contract) currentGrants, then grants = taskGrants request.profile authority (src/Max/DB/Task/Workflow.hs: beginAgentStep); since taskGrants (src/Max/Task/Types.hs) only filters by name and never adds grants or changes contract values, the child can hold no grant, contract, or profile capability the parent lacks — even if currentGrants were inflated, authority is still bounded by the parent's stored task.grants","Delegation is permitted only when all guards hold in beginAgentStep: authorizeWithin turn (ExecutionWork CheckOnly) succeeds, no steering is pending, the parent is a root task (isNothing task.parent), and the live turn's task_start grant contract equals the parent task's stored task_start contract; otherwise it rejects with 'agent() requires a live root task with delegation authority' or 'workflow_steering_pending' (src/Max/DB/Task/Workflow.hs)","The root-only guard (isNothing task.parent) makes delegation non-recursive: a child, being a non-root task, would fail the same guard if it tried agent(), so delegation depth cannot be used to accumulate authority across generations (src/Max/DB/Task/Workflow.hs)","Profile escalation is explicitly rejected: required = Research->Nothing, Browser->'browser_navigate', Sandbox->'sandbox_exec', Operations->'maxops_execute'; if that gate grant is absent from the computed grants, beginAgentStep returns 'requested profile exceeds the parent capability ceiling' (src/Max/DB/Task/Workflow.hs)","Within the grant ceiling, admission can still reject: admitTaskWithin ... grants is called with the already-intersected+profile-filtered grants and failures surface via admissionErrorText; the module header states admission, fencing, and budgets remain owned by the task subsystem (src/Max/DB/Task/Workflow.hs)","Step reuse is fenced by generation: a cached workflow_agent_steps row whose child is missing, revision-mismatched, or cancelled is rejected with 'cached child generation was invalidated; use a changed objective/input after inspecting its effects', and pollAgentStep re-checks authorizeWithin plus task.revision == step.childRevision, otherwise 'parent or child execution was cancelled, superseded or expired' (src/Max/DB/Task/Workflow.hs)","Awaited children are containment-restricted: workflowAllowed returns false for any turn that appears as a child in workflow_agent_steps, and the header comment states an awaited child may only run the normal agent loop, even after crash/restart (src/Max/DB/Task/Workflow.hs)","The base taskGrants allowlist (web_search, maxops_operations, maxops_query, context/memory/view/skill grants, task_start, task_status, task_list, task_steer) is available to every profile including Research, but only when the parent holds the identical grant+contract, so a child can at most mirror the parent's base capabilities; profile-specific grants (browser_*, sandbox_*, maxops_execute) are the only per-profile additions (src/Max/Task/Types.hs)","Evidence limit: authorizeWithin, admitTaskWithin, loadTask/loadAttempt, agentReport, and the construction of currentGrants live in modules not provided (Max.DB.Task.Authorization, Max.DB.Task.Admission, Max.DB.Task.Record), so authorization/admission semantics (budgets, fencing, live-status checks) are verified only by their use as gates","Evidence limit: the provided files do not show admitTaskWithin setting the child task's parent pointer to the delegating task; the no-nested-delegation property depends on children being non-root, which is enforced only via the isNothing task.parent guard in beginAgentStep"],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"status":"succeeded","summary":"Audit: agent() cannot widen its parent's authority, by construction in the two provided files. (1) Intersection cap: beginAgentStep (src/Max/DB/Task/Workflow.hs) computes authority = filterWithKey (name,contract-equal to parent's stored task.grants) over the live turn's currentGrants, then grants = taskGrants profile authority (src/Max/Task/Types.hs). taskGrants only filters by grant name against the per-profile allowlist (Research: base only; plus browser_* / sandbox_* / maxops_execute per profile); it never adds names or alters contract values. So the child's grant set is an exact (name+contract) subset of both the parent task's stored grants and the live turn's grants — no grant or contract can be widened, even if currentGrants were inflated. (2) Delegation preconditions (all in beginAgentStep): authorizeWithin turn (ExecutionWork CheckOnly) must succeed; steering must not be pending; the parent must be a root task (isNothing task.parent) — making delegation root-only so children cannot chain-delegate; the live turn must hold \"task_start\" with a contract value identical to the parent task's stored one. Any failure rejects with \"agent() requires a live root task with delegation authority\" or \"workflow_steering_pending\". (3) Profile ceiling: required gate grant (Browser→browser_navigate, Sandbox→sandbox_exec, Operations→maxops_execute, Research→none) must be present in the computed grants, else rejection \"requested profile exceeds the parent capability ceiling\". (4) Fencing/generation: cached workflow_agent_steps rows are reused only when the child's revision matches and it is not cancelled, else \"cached child generation was invalidated...\"; pollAgentStep re-runs authorizeWithin and requires task.revision == step.childRevision, else \"parent or child execution was cancelled, superseded or expired\"; admission via admitTaskWithin can still reject within the ceiling (budgets/fencing owned by the task subsystem). (5) Containment: workflowAllowed denies ordinary workflows on turns with awaited child steps, restricting children to the agent loop. Limits of the evidence (see unresolved): authorizeWithin, admitTaskWithin, loadTask/loadAttempt, agentReport, and the provenance of the currentGrants parameter are in modules not provided, and the provided files do not show admitTaskWithin setting the child's parent pointer, which the no-nesting property depends on.","unresolved":["authorizeWithin (Max.DB.Task.Authorization) was not provided; its exact live-authorization semantics are inferred only from its use as a gate in beginAgentStep/pollAgentStep","admitTaskWithin (Max.DB.Task.Admission) was not provided; admission rules (budgets, fencing, owner checks) and whether it sets the child task's parent pointer are inferred from the module header and the isNothing task.parent guard only","The provenance of the currentGrants parameter (how the live turn grant map is constructed) is outside the provided files","loadTask/loadAttempt (Max.DB.Task.Record), agentReport, and taskIsLive were not provided; result settlement semantics in pollAgentStep are verified only at the grant/revision level","No production deployment or performance properties were claimed or verified; evidence is static source only"]},"status":"succeeded"},{"report":{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"payload":{"claims":["Shared budget counters are the durable_tasks columns calls_reserved/rounds_reserved, read by Record.hs loadTask (FOR UPDATE) as calls/rounds with max_calls/max_rounds, documented as 'Reserved usage, including work still in flight'; Authorization.hs reserves with `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` on (task, root) where root = loadTask(fromMaybe task.taskId task.root).","Two awaited siblings cannot overspend: they share the parent's conversation_id and inherited root_task_id (Admission.hs rootId = fromMaybe task.taskId task.root), and authorizeWithin linearizes on the conversation commit lock (lockTurnConversation: `FOR UPDATE OF c` on conversations, ConversationLock.hs) plus the root row's FOR UPDATE, so the second sibling re-reads the incremented counter under lock.","The cap is exact: guard `not (available reservation task && available reservation budget) -> pure False` with `available ReserveCall t = t.calls < t.maxCalls` (strict <) and a +1 increment bounds calls_reserved at max_calls (same for rounds); a self-rooted task's duplicated IN-list updates one row once.","Reservations are additionally refused on deadline (`task.deadline <= now || budget.deadline <= now`), non-live budget (`taskIsLive budget.status`), stale attempt (revision/attempt/leaseUntil mismatch), or drifted/terminated ancestors (`ancestors == [Only False]` recursive check), so a cancelled parent or stale sibling attempt cannot reserve.","Scheduling capacity (Scheduling.hs eligibleTask, params 40/10/10 = global/per-conversation/per-owner) counts only `active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(workflow_agent_waits turn_id=execution.turn_id)`, so a parent in status 'waiting' and a running parent registered in workflow_agent_waits hold zero capacity slots while awaiting siblings.","Waiting parents cannot deadlock capacity: every counted holder carries a 60s lease (claimTask inserts task_attempts lease_until = addUTCTime 60 now), expires out of the counts, becomes reclaimable, and claimTask fences expired attempts (task->'queued', journal->'outcome-unknown', turn->'crashed', settleTurn SettlementFailed); tasks also die on deadline or attempt>=40 (completeTask BudgetExhausted), so a full-capacity state cannot persist.","Capacity accounting is serialized by pg_advisory_xact_lock(872008) held for claimTask's whole withTransaction ('Shared with all claimers, so capacity checks and reservation commit once'), with a second re-check of eligibleTask under lockTaskConversation, preventing concurrent below-cap observations and double-claims.","Residual bounded risks, not deadlocks: non-workflow_agent_waits synchronous blocks pin a slot until the 60s lease (then work is crashed); Admission.hs rejects new sibling admission at >=160 queued/running/waiting/retrying tasks per conversation (AdmissionQueueFull, counts waiting parents); reservation release lives in Max.DB.Task.Settlement (not provided), so release of in-flight reservations is unverified from the provided files."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"status":"succeeded","summary":"Audit of shared-budget overspend and waiting-parent capacity deadlock, based on the five provided modules.\n\nQ1 — Can two awaited siblings overspend the shared call/round budget? NO, per the code.\n- Authorization.hs `authorizeWithin` reserves against the executing task row AND the budget row: `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` (taskId, rootTaskId), where `root <- loadTask (fromMaybe task.taskId task.root)`. Record.hs projects `calls`/`rounds` from the `calls_reserved`/`rounds_reserved` columns and documents them as \"Reserved usage, including work still in flight\", so in-flight reservations count against the cap.\n- Linearization: `authorizeWithin` first acquires the conversation commit lock `lockTurnConversation` (`SELECT ... FROM conversations c JOIN agent_turns t ... FOR UPDATE OF c`, ConversationLock.hs) and only then row-locks turn/task/root via `loadTask`/`loadAttempt` (`... FOR UPDATE`, Record.hs). Siblings admitted by the same parent share the parent's `conversation_id` and inherit the same `root_task_id` (Admission.hs: `rootId = (\\task -> fromMaybe task.taskId task.root) <$> parent`), so two sibling reservations serialize on the same conversations-row lock and the same root row lock; the second sibling re-reads `calls_reserved` after acquiring the lock and sees the first's increment.\n- Exact cap: the guard is `not (available reservation task && available reservation budget) -> pure False` with `available ReserveCall t = t.calls < t.maxCalls` (strict <) and a +1 increment, so `calls_reserved` can reach `max_calls` but not exceed it (same for rounds); for a self-rooted task the duplicated IN-list updates one row once. Admission inserts `max_calls=200, max_rounds=400` per child, so each sibling is also individually capped, and the shared root cap additionally bounds their sum.\n- Denial gates: reservation is refused if `task.deadline <= now || budget.deadline <= now`, if `not (taskIsLive budget.status)`, if the attempt is stale (`task.revision == execution.revision && task.attempt == execution.attempt && execution.leaseUntil > now`), or if the recursive ancestor check `ancestors == [Only False]` fails (any ancestor not in 'running','queued','waiting','retrying' or revision-drifted). A cancelled parent or stale attempt therefore cannot reserve.\n\nQ2 — Can waiting parents deadlock task capacity? NO permanent deadlock in the provided code.\n- Scheduling.hs `eligibleTask` enforces three counters with params `(40, 10, 10)`: global < 40, per-conversation < 10, per-owner < 10, where every counted holder must satisfy `active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits waiting WHERE waiting.turn_id=execution.turn_id)`.\n- A parent in status 'waiting' (blocked on its children) is excluded by the `status='running'` requirement; a parent still 'running' but registered in workflow_agent_waits is excluded by the NOT EXISTS. So a parent awaiting two siblings occupies none of the 40/10/10 slots, and children of a waiting parent remain claimable (`parent.status IN ('queued','running','waiting','retrying')` gate).\n- Bounded hold: `claimTask` writes `task_attempts ... lease_until = addUTCTime 60 now`, so any counted holder expires out of all three counts within 60s and becomes reclaimable (`status='running' AND NOT EXISTS (live lease)`); `claimTask` then fences the expired attempt (task→'queued', journal→'outcome-unknown'/'task_lease_expired', turn→'crashed', `settleTurn ... SettlementFailed`). Tasks also terminate on `task.deadline <= now || task.attempt >= 40` → `completeTask ... BudgetExhausted`. A full-capacity state therefore cannot persist.\n- Capacity decisions are serialized: `claimTask` runs in `withTransaction` and first takes `pg_advisory_xact_lock(872008)` (\"Shared with all claimers, so capacity checks and reservation commit once\"), then re-evaluates `eligibleTask (Just identifier)` under `lockTaskConversation`, so two workers cannot both observe a stale below-cap count and double-claim.\n\nCaveats (see unresolved): reservation release lives in Max.DB.Task.Settlement (imported by Scheduling.hs but not provided); Admission.hs's 160-per-conversation queue cap counts 'waiting' parents and can reject new sibling admission (AdmissionQueueFull, a hard error, not a hang); synchronous (non-workflow_agent_waits) blocks pin a slot only until the 60s lease, at which point in-flight work is crashed; rows with NULL root_task_id would degrade to a per-task cap; children of a terminated parent are simply never claimed by eligibleTask and no reaping code is in the provided files.","unresolved":["Reservation release (decrement of calls_reserved/rounds_reserved) is in Max.DB.Task.Settlement, which Scheduling.hs imports but which was not provided; if settlements never release, long-running workflows self-starve at the root cap.","A synchronous block inside a turn that is not recorded in workflow_agent_waits (e.g. a long tool call) still pins a capacity slot for up to the 60s lease and the work is then crashed (settleTurn SettlementFailed) — bounded stall, but unobservable in the provided files.","Children of a parent that leaves ('queued','running','waiting','retrying') become permanently unclaimable by eligibleTask; no reaping/cancellation cascade for orphaned children appears in the provided modules.","Rows with NULL root_task_id (legacy tasks) use their own row as budget, so such siblings do not share a cap; all rows admitted via Admission.hs set root_id.","Lock ordering relies on callers holding the conversation lock before task-row locks (Record.hs header); any caller violating that order could deadlock on conversations-row vs durable_tasks-row locks, but all provided callers follow it."]},"status":"succeeded"},{"report":null,"status":"running"}],"completed":false,"completion_tokens":19154,"mode":"serial","model_calls":6,"prompt_tokens":26517,"seconds":300.010575,"source_reads":11,"started_at":"2026-09-12T09:32:29.561118Z","usage_records":6,"workflow_output":null},{"actual_models":["qwen3.8-27b"],"children":[{"report":{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"payload":{"claims":["Child grants are computed as an intersection: authority = Map.filterWithKey same currentGrants where same name contract = Map.lookup name task.grants == Just contract, so a child receives only (name, contract) pairs present in both the caller's currentGrants and the parent task's DB-loaded task.grants with exact string equality per name (source:src/Max/DB/Task/Workflow.hs).","grants = taskGrants request.profile authority then intersects the profile whitelist from source:src/Max/Task/Types.hs (base names incl. task_start, maxops_operations, maxops_query, web_search; Operations adds maxops_execute; Browser adds browser_* and view_zhihu; Sandbox adds sandbox_*, import_file_to_sandbox, nix_search; Research adds nothing), so child grants ⊆ parent task.grants ∩ profile-allowed names; even an inflated caller-supplied currentGrants cannot exceed the parent's recorded grants because the filter anchors on loadTask(task.grants), so agent() cannot widen the parent's authority.","Rejection point 1: any beginAgentStep guard failure — authorizeWithin turn (ExecutionWork CheckOnly) not passing, parent not a live root (isNothing task.parent), or task_start grant missing/mismatched (Map.lookup \"task_start\" currentGrants must equal Map.lookup \"task_start\" task.grants) — falls to the catchall Left \"agent() requires a live root task with delegation authority\" (source:src/Max/DB/Task/Workflow.hs).","Rejection point 2: workflowSteeringPending turn (an unprocessed 'steer' event for the turn's live attempt) → Left \"workflow_steering_pending\" (source:src/Max/DB/Task/Workflow.hs).","Rejection point 3: profile capability ceiling — if the requested profile's required marker grant (browser_navigate / sandbox_exec / maxops_execute; Research requires none) is absent from the computed child grants → Left \"requested profile exceeds the parent capability ceiling\" (source:src/Max/DB/Task/Workflow.hs).","Rejection points 4-5: admitTaskWithin failure → Left (admissionErrorText failure); an invalidated cached child generation → Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\" (source:src/Max/DB/Task/Workflow.hs).","Rejection point 6: pollAgentStep requires authorizeWithin to pass and task.revision == step.childRevision on the loaded child; otherwise Left \"parent or child execution was cancelled, superseded or expired\"; endAgentWait only deletes the workflow_agent_waits row and grants nothing (source:src/Max/DB/Task/Workflow.hs).","No recursive escalation: the delegation gate requires isNothing task.parent, so a child (non-root) task can never call agent() again, and workflowAllowed returns False for a child's turn via NOT EXISTS over workflow_agent_steps joined to that child's task_attempts, restricting children to the normal agent loop and blocking ordinary workflows on child turns (source:src/Max/DB/Task/Workflow.hs).","Limits of the evidence: authorizeWithin (Max.DB.Task.Authorization), admitTaskWithin (Max.DB.Task.Admission), loadTask/loadAttempt (Max.DB.Task.Record) and taskIsLive (Max.Task.State) are imported by the workflow module but not provided, so authorization policy, admission/budget/fencing and liveness internals are unverified (the module header says admission, fencing and budgets remain owned by the task subsystem); grant-name-to-tool enforcement on the child turn and agent()'s tool exposure are outside the two audited files; contract values are opaque Text compared by string equality; the taskGrants base list (incl. maxops_operations/maxops_query for every profile, even Research) is a subset of parent grants, not a widening, but broader than the profile name suggests (source:src/Max/Task/Types.hs)."],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"status":"succeeded","summary":"Audit of whether agent() can widen its parent's authority, over src/Max/DB/Task/Workflow.hs and src/Max/Task/Types.hs.\n\nFINDING: No widening is possible in this code path. The child's grants are computed as a strict intersection: beginAgentStep computes authority = Map.filterWithKey same currentGrants where `same name contract = Map.lookup name task.grants == Just contract`, i.e. only (name, contract) pairs present in BOTH the caller's currentGrants and the parent task's DB-loaded task.grants (exact string equality per name; a differing contract under the same name fails the match). grants = taskGrants request.profile authority then further intersects with the per-profile whitelist from Max.Task.Types (base 17 names incl. task_start/task_status/task_list/task_steer, maxops_operations, maxops_query, web_search etc.; plus Operations→maxops_execute, Browser→browser_* + view_zhihu, Sandbox→sandbox_* + import_file_to_sandbox + nix_search; Research adds nothing). Because the filter is anchored on task.grants loaded via loadTask (not on the caller-supplied currentGrants), even an inflated currentGrants cannot exceed the parent's recorded grants.\n\nACTUAL REJECTION POINTS (beginAgentStep, the agent() entry): (1) any guard failure — authorizeWithin turn (ExecutionWork CheckOnly) failing, no root parent (isNothing task.parent fails), or the task_start grant missing/mismatched (Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants required) — collapses to Left \"agent() requires a live root task with delegation authority\"; (2) pending steering → Left \"workflow_steering_pending\"; (3) profile ceiling: the profile's required marker grant (browser_navigate / sandbox_exec / maxops_execute; none for Research) missing from the computed child grants → Left \"requested profile exceeds the parent capability ceiling\"; (4) admitTaskWithin failure → Left (admissionErrorText failure); (5) invalidated cached child generation → Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\". In pollAgentStep: child not loadable, authorizeWithin failing, or revision mismatch → Left \"parent or child execution was cancelled, superseded or expired\". endAgentWait only deletes a wait row (no authority effect).\n\nNO RECURSIVE ESCALATION: the parent must be a live root task (isNothing task.parent), so a child task can never pass the agent() gate again; workflowAllowed returns False for a child's turn (NOT EXISTS over workflow_agent_steps joined to the child's task_attempts), so the child runs only the normal agent loop and cannot run ordinary workflows or re-delegate. The child may still receive a task_start grant (base list) equal to the parent's, but it is unusable by the child due to the root-parent gate.\n\nEVIDENCE LIMITS: authorizeWithin (Max.DB.Task.Authorization), admitTaskWithin (Max.DB.Task.Admission), loadTask/loadAttempt (Max.DB.Task.Record), taskIsLive (Max.Task.State) are imported but not provided, so authorization policy, admission/budget/fencing internals, and liveness semantics are unverified (module header states admission, fencing and budgets remain owned by the task subsystem). Grant-name→tool enforcement on the child turn, and agent()'s tool exposure/registration, are outside the two audited files. Contract values are opaque Text compared by string equality. Base grant list is broad (maxops_operations/maxops_query available to every profile incl. Research) — a subset of the parent's grants, not widening, but wider than the profile name implies.","unresolved":["Max.DB.Task.Authorization (authorizeWithin) implementation not provided; authorization policy for ExecutionWork CheckOnly unverified","Max.DB.Task.Admission (admitTaskWithin) not provided; admission/budget/fencing checks applied to the child unverified","Max.DB.Task.Record (loadTask/loadAttempt) and Max.Task.State (taskIsLive) not provided; liveness/supersession semantics inferred only from usage","Enforcement of the child's grant map at tool dispatch on the child turn is not in the audited files","agent() tool exposure/registration to the model is not in the audited files"]},"status":"succeeded"},{"report":{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"payload":{"claims":["Shared-budget enforcement (Authorization.hs): every ExecutionWork reservation is gated on `available reservation task && available reservation budget` (available ReserveCall = `task.calls < task.maxCalls`, available ReserveRound = `task.rounds < task.maxRounds`), then applied as `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` (or rounds_reserved) updating BOTH the child row and the shared root row inside the caller's pinned transaction; root is `loadTask (fromMaybe task.taskId task.root)` and admission propagates the parent's root to every child, so all siblings share one counter pair (max_calls=200, max_rounds=400 set at insert in Admission.hs).","Serialization of two sibling authors (ConversationLock.hs + Record.hs): both siblings' attempt turns live in the same conversation (children inherit the caller turn's conversation_id in Admission.hs; claimTask creates sibling attempt turns in task.conversationId in Scheduling.hs). authorizeWithin first takes lockTurnConversation = `SELECT c.conversation_id ... FOR UPDATE OF c`, so the two siblings' authorization transactions are serialized on the conversations row; independently loadTask takes `SELECT ... FROM durable_tasks ... FOR UPDATE` on both the child row and the root row, so the second sibling re-reads the first sibling's already-committed calls_reserved/rounds_reserved. The check-and-increment is therefore atomic with respect to the shared root budget: no TOCTOU overspend.","Waiting siblings cannot add spend (Record.hs + Authorization.hs): TaskRecord.calls/rounds decode from calls_reserved/rounds_reserved, documented as 'Reserved usage, including work still in flight', so budget held by an in-flight or waiting sibling is already inside the shared root counters; and the authorization guard requires `task.status == Running` plus matching revision/attempt and a live lease, so a sibling parked in `waiting` cannot increment any counter at all. Two awaited siblings can hold previously reserved budget but cannot exceed the shared root's max_calls/max_rounds.","Claim path is globally serialized and re-validated (Scheduling.hs): claimTask takes `pg_advisory_xact_lock(872008)` shared by all claimers, then lockTaskConversation, then re-runs `eligibleTask (Just identifier)` under lock before starting the attempt, so a candidate changed while waiting for its conversation lock is re-evaluated and the capacity check + reservation commit once.","Waiting parents hold zero capacity and cannot deadlock the queue (Scheduling.hs): all three capacity predicates in eligibleTask count only `active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits ...)` with caps 40 global / 10 per conversation / 10 per owner; a task in `waiting` status is excluded from every count (status is not 'running'), and running tasks with a live workflow_agent_waits row are also excluded. A parent parked in `waiting` therefore consumes no capacity slot.","No capacity slot is held indefinitely (Scheduling.hs): each attempt gets a 60s lease (`addUTCTime 60 now` into task_attempts.lease_until); the advisory-locked pass fences expired attempts (execution_journal -> 'outcome-unknown', agent_turns -> 'crashed', task -> 'queued', attempt+1), and `task.attempt >= 40` or `deadline <= now` forces terminal completeTask with BudgetExhausted. Stuck waiting/retrying tasks become claimable via `work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp()`, so an un-woken parent is bounded by retry exhaustion, not deadlock.","Children of a waiting parent stay eligible (Scheduling.hs): the parent gate is `work.parent_task_id IS NULL OR EXISTS (parent.status IN ('queued','running','waiting','retrying'))`, which includes 'waiting', so a parent parked awaiting its children does not freeze its children, and their completion frees the parent for re-claim.","Lock order is uniform and deadlock-free (ConversationLock.hs, Record.hs, Authorization.hs, Scheduling.hs): advisory lock (claim path only) -> conversations row (lockTurnConversation/lockTaskConversation, FOR UPDATE OF c) -> durable_tasks rows (loadTask FOR UPDATE) -> task_attempts row (loadAttempt FOR UPDATE); all claimers are serialized by the global advisory lock 872008, so no circular wait can form between conversation, task, attempt, or scheduler locks.","Residual risk A (over-commit, not deadlock): a stale workflow_agent_waits row on a live running task makes the capacity predicates under-count, permitting more than 40/10/10 concurrent runs; the wait-row removal/settlement logic lives in Max.DB.Task.Settlement, which is imported by Scheduling.hs but was not provided, so prompt cleanup on child completion or crash is not verifiable from the given files.","Residual risk B (zombies, not deadlock or overspend): a queued child whose parent becomes terminal is permanently ineligible under the Scheduling.hs parent gate (no deadline branch covers status='queued'); cleanup of such orphans presumably sits in the missing Settlement cascade and could not be confirmed from the provided files.","By-design note: ReserveCall and ReserveRound are independent counter pairs (calls_reserved vs rounds_reserved vs max_calls=200 vs max_rounds=400), so a task may hold up to 200 calls AND 400 rounds at the root simultaneously; the deadline guard `task.deadline <= now || budget.deadline <= now -> False` additionally stops all further reservations once either the task's or the shared budget's deadline passes."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"status":"succeeded","summary":"Audit of the five provided sources. Verdict: (1) Two awaited siblings CANNOT overspend the shared call/round budget; the check-and-increment is atomic under overlapping locks and the counters are reservation-based. (2) Waiting parents CANNOT deadlock task capacity: waiting tasks are excluded from every capacity predicate, every running attempt is time-boxed by a 60s lease with a 40-attempt cap, and deadline expiry makes stuck waiting tasks reclaimable. Residual risks noted are capacity under-enforcement (stale wait rows) and orphaned queued children — not overspend or deadlock — and both depend on Settlement.hs which was not provided.\n\nBudget-side details (Authorization.hs, Record.hs, Admission.hs, ConversationLock.hs):\n- authorizeWithin gates every ExecutionWork on `available reservation task && available reservation budget` (ReserveCall: `calls < maxCalls`; ReserveRound: `rounds < maxRounds`), then applies `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` (same for rounds), updating the child row and the shared root row in the caller's pinned transaction.\n- The budget row is `root <- loadTask (fromMaybe task.taskId task.root)`; admission propagates the parent's root to every child (`rootId = (…fromMaybe task.taskId task.root) <$> parent`), so all siblings share one counter pair. max_calls=200 / max_rounds=400 are set at insertion.\n- Siblings share one conversation: children inherit the caller turn's conversation_id (Admission.hs); claimTask creates the sibling's attempt turn in `task.conversationId` (Scheduling.hs). authorizeWithin first takes `lockTurnConversation turn` = `…FOR UPDATE OF c` on the conversations row (ConversationLock.hs), serializing both siblings' auth transactions; independently, loadTask takes `SELECT … FROM durable_tasks … FOR UPDATE` on child and root rows (Record.hs), so a second sibling re-reads the first sibling's incremented calls_reserved/rounds_reserved after commit. No TOCTOU overspend.\n- TaskRecord.calls/rounds decode from calls_reserved/rounds_reserved — \"Reserved usage, including work still in flight\" (Record.hs) — so budget held by an in-flight or waiting sibling is already inside the shared root counters. The authorization guards also require `task.status == Running`, so a sibling parked in `waiting` cannot increment any counter; it can only spend what it already reserved.\n- Two siblings can hold budget simultaneously only via prior reservations, which are already counted on the shared root row; combined with serialization, the shared 200/400 cap is globally enforced across the sibling set.\n\nCapacity-side details (Scheduling.hs):\n- claimTask takes `pg_advisory_xact_lock(872008)` (shared by all claimers) so the capacity check and reservation commit exactly once per claim; it then takes lockTaskConversation and re-runs `eligibleTask (Just identifier)` under lock, closing the TOCTOU window on eligibility.\n- All three capacity predicates in eligibleTask count only `active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits …)` — caps 40 global, 10 per conversation, 10 per owner. A task in `waiting` status matches none of these (status is not 'running'), and a running task with a live wait row is excluded, so a waiting parent holds ZERO capacity slots and cannot deadlock the queue.\n- No slot is held indefinitely: each attempt gets a 60s lease (`addUTCTime 60 now` into task_attempts.lease_until); the same advisory-locked pass fences expired attempts (execution_journal → 'outcome-unknown', agent_turns → 'crashed', task → 'queued', attempt+1); `task.attempt >= 40` or `deadline <= now` forces terminal `completeTask … BudgetExhausted`. A stuck waiting/retrying task becomes eligible via `work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp()`, so even an un-woken parent is bounded by retry exhaustion rather than deadlock.\n- The parent liveness gate includes 'waiting' (`parent.status IN ('queued','running','waiting','retrying')`), so children of a waiting parent remain claimable and can complete to unblock it.\n- Lock order is uniform: advisory (claim path only) → conversations row (lockTurnConversation/lockTaskConversation) → durable_tasks rows (loadTask FOR UPDATE) → task_attempts row (loadAttempt FOR UPDATE); no circular wait exists between claimers (global advisory lock) or between authorization paths (conversation-first), so the parent/child/wait machinery cannot deadlock on locks.\n\nResidual findings (not overspend, not capacity deadlock):\n- Stale workflow_agent_waits rows would make the capacity counts undercount, permitting over-commit beyond 40/10/10 (opposite direction from deadlock). Wait-row hygiene lives in Max.DB.Task.Settlement, imported by Scheduling.hs but not in the provided file set, so prompt removal on child completion / crash settlement is not verifiable from the given files.\n- A `queued` child whose parent becomes terminal is permanently ineligible (parent gate) with no deadline branch for 'queued' — a zombie-task liveness gap, presumably handled by the missing Settlement cascade; also not verifiable here.\n- ReserveCall and ReserveRound are independent counter pairs, so a task can hold up to 200 calls AND 400 rounds at the root — by design, not overspend; the `task.deadline <= now || budget.deadline <= now` guard further stops all reservations after deadlines.","unresolved":["Max.DB.Task.Settlement is imported by Scheduling.hs but not provided; cannot verify that workflow_agent_waits rows are removed promptly on child completion or crash (stale rows would under-count capacity, i.e., over-commit, not deadlock).","Cannot verify orphan handling for queued children of terminal parents (permanently ineligible under the Scheduling.hs parent gate); settlement/cascade logic is outside the provided file set.","taskIsLive and task status transition definitions live in Max.Task.State (not provided); assumed to mean non-terminal statuses as used in the ancestor-liveness check."]},"status":"succeeded"},{"report":{"evidence":["source:src/Max/Task/WorkflowRuntime.hs","source:src/Max/CodeMode/Execution.hs","source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Delegation.hs"],"payload":{"claims":["Reuse identity: beginAgentStep matches workflow_agent_steps rows by parent_task_id + parent_revision + call_key, where call_key = 'agent:' + fingerprint({receipts, grants, request}) (agentCallKey in Delegation.hs); receipts = JS runtime version + per-skill load versions (WorkflowRuntime.hs), grants = taskGrants(profile, parent grants ∩ current grants).","Editing a source step reuses: a source edit creates only a new journal step (new step_key/source_fingerprint via boundHooks.ehStart in CodeMode/Execution.hs); because source_fingerprint is not part of agentCallKey, an unchanged agent() request + receipts reuses the prior child task and its settled report, per Delegation.hs's module doc.","Cache hit: child revision unchanged and status /= Cancelled re-joins the child; still Queued/Running/Retrying re-registers workflow_agent_waits and keeps polling; settled children return the DB-cached report with a committed receipt carrying reused:true and original_execution = journal#COALESCE(settled_journal_id, first_journal_id).","Invalidation: child revision change or Cancelled status rejects admission with 'cached child generation was invalidated'; receipts (skill package or runtime version), grants, profile, or request changes force a fresh child; a parent revision bump stops old rows from ever matching; workflowAllowed prevents a child turn from running another workflow.","Steering boundary points: (1) admission — beginAgentStep rejects with 'workflow_steering_pending' when an unconsumed steer event (event_id > GREATEST(work.consumed_event, attempt.seen_event) on the current attempt generation) exists; the runtime passes that code through so steeringOutcome fires; (2) polling — WorkflowRuntime's await checks workflowSteeringPending after each empty poll and commits {interrupted:true, reason:'workflow_steering_pending'} to stop the 250 ms re-poll loop.","Wasm host stop on steering: runWasmProgram sets WasmHostStopped, emits the interrupted batch envelopes as the run value, and codeModeInvocation reports ToolSucceeded (ContinueLoop), so steering surfaces to the model as a success value rather than a fault.","Cancellation gap: pollAgentStep returns Right Nothing for a terminal child (e.g. Cancelled) with no result report, which await() treats as 'still running' and re-polls indefinitely (250 ms) until a steering event or parent authorization failure — no terminal outcome exists for report-less cancelled children.","Cancellation gap: all poll failures (cancelled, superseded, expired, lost authorization) collapse to one Left and the runtime throws TaskCancelled for each, making expiration/supersession indistinguishable from user cancellation to host and model.","Provenance gap: source_fingerprint/step_key are journal-entry-only metadata absent from the reuse key, so distinct source steps with identical agent args + receipts share one cache entry; the reuse receipt lacks the original execution's source fingerprint, and the reuse run's step_key reflects the new source, not the source that produced the reused data.","Provenance gap: original_execution points at a parent-side journal (COALESCE(settled_journal_id, first_journal_id)), not the child's own execution journal; the receipt exposes only the child task id, so per-child provenance rests on the child report's evidence fields.","Steering blind spot: workflowSteeringPending matches only the current attempt generation (revision/attempt equality with the work row); a stale attempt row returns false, so pending steers are not a boundary and the next poll instead fails authorization and surfaces as TaskCancelled.","Orphaned waits: markWait inserts workflow_agent_waits and endAgentWait deletes only via the runtime's finally; a crash between admission and wait deletion leaves orphan rows with no cleanup visible in the provided files.","Steering visibility: steering is observed only at agent admission or while polling an agent child; steers consumed by the parent before child admission (event_id <= both horizons) never interrupt a running child, and no steering boundary applies to plain-tool batches in these files."],"sources":["source:src/Max/Task/WorkflowRuntime.hs","source:src/Max/CodeMode/Execution.hs","source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Delegation.hs"]},"status":"succeeded","summary":"Audit of cache reuse, invalidation, and steering boundaries across WorkflowRuntime.hs, CodeMode/Execution.hs, DB/Task/Workflow.hs, and Delegation.hs.\n\nCACHE REUSE\n- Reuse identity: beginAgentStep (DB/Task/Workflow.hs) scopes the workflow_agent_steps lookup by parent_task_id + parent_revision + call_key; call_key = agentCallKey (Delegation.hs) = \"agent:\" + fingerprint({receipts, grants, request}), where request is the full AgentRequest (objective, inputs, profile name, output_contract) and the host's receipts value (WorkflowRuntime.hs) = javaScriptRuntimeVersion + per-skill versions from toolSkillLoads. Grants are folded in as taskGrants(profile, authority) where authority is the intersection of the parent's grants with the current turn's grants.\n- Hit path: if a prior row exists and the child's revision is unchanged and status /= Cancelled, the call re-joins the existing child (no new admission). If the child is still Queued/Running/Retrying it re-registers workflow_agent_waits (markWait) and the runtime keeps polling; if it settled, await immediately returns the DB-cached report as a committed receipt with reused:true and original_execution = journal#COALESCE(settled_journal_id, first_journal_id).\n- What editing a source step reuses: per Delegation.hs's module doc, a source edit creates a new journal step, but \"unchanged semantic calls can still reuse their child in the same parent revision.\" In CodeMode/Execution.hs the source identity (source_fingerprint = sha256(wpEvidence), step_key = sha256({source, args})) is recorded only in boundHooks.ehStart journal-entry metadata; it is NOT part of agentCallKey. So editing the workflow source while the agent() args and receipts are unchanged reuses the previous child task and its settled report — only the new execution's step_key/source_fingerprint change. Only receipt changes (skill package version, JS runtime version), request changes, or grant/profile changes create a new key.\n- Invalidation: (a) child revision changed or child status == Cancelled → admission rejected with \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\"; (b) receipts/grants change → key miss → fresh child via admitTaskWithin under the same key; (c) parent revision change → old rows never match again; (d) profile capability ceiling (browser_navigate/sandbox_exec/maxops_execute must be within parent grants), parent must be a live root task with matching task_start grants; (e) workflowAllowed blocks a child turn from starting another workflow (no nesting).\n\nSTEERING BOUNDARIES\n- Predicate: workflowSteeringPending (DB/Task/Workflow.hs) = a task_events row of kind 'steer' with event_id > GREATEST(work.consumed_event, attempt.seen_event), on the current attempt generation only (attempt.revision = work.revision AND attempt.attempt = work.attempt).\n- Two boundary points: admission (beginAgentStep rejects new agent steps with \"workflow_steering_pending\"; the runtime maps that code through verbatim so CodeMode's steeringOutcome — which matches either the rejected code or a committed {interrupted:true, reason:\"workflow_steering_pending\"} — fires) and polling (WorkflowRuntime's await checks after each empty poll and, if pending, commits the interrupted receipt and stops the 250 ms re-poll).\n- Wasm host stop: on a steering boundary runWasmProgram records the interrupted batch envelopes as cmOutput, finalExit = WasmHostStopped, and codeModeInvocation reports ToolSucceeded (ContinueLoop) — the model sees interrupted receipts as a success value, not a fault.\n\nCANCELLATION AND PROVENANCE GAPS\n1. Infinite poll of dead children: pollAgentStep returns Right Nothing (the \"still running\" path) for a child whose status is terminal (e.g. Cancelled) but has no result report, because the report guard requires Just report. await() then re-polls that dead child every 250 ms with no terminal escape other than a steering event or parent-side authorization failure. A child cancelled without a report can pin a parent wait forever.\n2. Failure conflation: every poll failure (parent or child cancelled, superseded, expired, or lost authorization) collapses to one Left; the runtime throws TaskCancelled for all of them, so expiration or supersession is indistinguishable to the host/model from user cancellation.\n3. Source provenance gap: source_fingerprint/step_key are journal-only metadata and are absent from the reuse key, so two different source steps with identical agent args + receipts share one cache entry. The reuse receipt carries original_execution (parent-side journal) but not the original execution's source fingerprint, and the reuse run's recorded step_key is that of the new source — the step key recorded at reuse does not match the source that produced the data.\n4. original_execution ambiguity: the receipt points at COALESCE(settled_journal_id, first_journal_id) — the parent-side journal that settled or first created the step, not the child's own execution journal; the receipt exposes only the child task id, so per-child provenance depends entirely on the child report's evidence fields.\n5. Stale-generation steering blind spot: workflowSteeringPending matches only the current attempt generation; if the attempt row is stale relative to the work row it returns false, so pending steers are not a boundary — the next poll instead fails authorization and surfaces as TaskCancelled.\n6. Orphaned waits: markWait inserts workflow_agent_waits and endAgentWait deletes only via the runtime's finally; a crash or unclean exit between admission and wait deletion leaves orphan rows, with no cleanup visible in these files.\n7. Steering visibility scope: steering is observed only at agent admission or while polling an agent child; steer events consumed by the parent before a child is admitted (event_id <= both horizons) never interrupt a running child, and no steering boundary applies to plain-tool batches in these files.\n\nCaveats: loadTask/loadAttempt/admitTaskWithin and the worker's cancellation writer are not in the provided files, so items 1, 5, and 6 are inferred from the visible call graph rather than directly observed.","unresolved":["Worker-side cancellation semantics (how a child becomes Cancelled without a report) live outside the four provided files; gap #1's reachability depends on it.","loadTask/loadAttempt/admitTaskWithin implementations are not provided; stale-generation behavior (gap #5) is inferred from the SQL predicates alone.","No cleanup job for orphaned workflow_agent_waits rows is visible in the provided files."]},"status":"succeeded"}],"completed":true,"completion_tokens":32555,"mode":"parallel","model_calls":8,"prompt_tokens":59907,"seconds":170.027474,"source_reads":12,"started_at":"2026-09-12T09:37:29.596101Z","usage_records":8,"workflow_output":[{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"findings":"Audit of whether agent() can widen its parent's authority, over src/Max/DB/Task/Workflow.hs and src/Max/Task/Types.hs.\n\nFINDING: No widening is possible in this code path. The child's grants are computed as a strict intersection: beginAgentStep computes authority = Map.filterWithKey same currentGrants where `same name contract = Map.lookup name task.grants == Just contract`, i.e. only (name, contract) pairs present in BOTH the caller's currentGrants and the parent task's DB-loaded task.grants (exact string equality per name; a differing contract under the same name fails the match). grants = taskGrants request.profile authority then further intersects with the per-profile whitelist from Max.Task.Types (base 17 names incl. task_start/task_status/task_list/task_steer, maxops_operations, maxops_query, web_search etc.; plus Operations→maxops_execute, Browser→browser_* + view_zhihu, Sandbox→sandbox_* + import_file_to_sandbox + nix_search; Research adds nothing). Because the filter is anchored on task.grants loaded via loadTask (not on the caller-supplied currentGrants), even an inflated currentGrants cannot exceed the parent's recorded grants.\n\nACTUAL REJECTION POINTS (beginAgentStep, the agent() entry): (1) any guard failure — authorizeWithin turn (ExecutionWork CheckOnly) failing, no root parent (isNothing task.parent fails), or the task_start grant missing/mismatched (Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants required) — collapses to Left \"agent() requires a live root task with delegation authority\"; (2) pending steering → Left \"workflow_steering_pending\"; (3) profile ceiling: the profile's required marker grant (browser_navigate / sandbox_exec / maxops_execute; none for Research) missing from the computed child grants → Left \"requested profile exceeds the parent capability ceiling\"; (4) admitTaskWithin failure → Left (admissionErrorText failure); (5) invalidated cached child generation → Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\". In pollAgentStep: child not loadable, authorizeWithin failing, or revision mismatch → Left \"parent or child execution was cancelled, superseded or expired\". endAgentWait only deletes a wait row (no authority effect).\n\nNO RECURSIVE ESCALATION: the parent must be a live root task (isNothing task.parent), so a child task can never pass the agent() gate again; workflowAllowed returns False for a child's turn (NOT EXISTS over workflow_agent_steps joined to the child's task_attempts), so the child runs only the normal agent loop and cannot run ordinary workflows or re-delegate. The child may still receive a task_start grant (base list) equal to the parent's, but it is unusable by the child due to the root-parent gate.\n\nEVIDENCE LIMITS: authorizeWithin (Max.DB.Task.Authorization), admitTaskWithin (Max.DB.Task.Admission), loadTask/loadAttempt (Max.DB.Task.Record), taskIsLive (Max.Task.State) are imported but not provided, so authorization policy, admission/budget/fencing internals, and liveness semantics are unverified (module header states admission, fencing and budgets remain owned by the task subsystem). Grant-name→tool enforcement on the child turn, and agent()'s tool exposure/registration, are outside the two audited files. Contract values are opaque Text compared by string equality. Base grant list is broad (maxops_operations/maxops_query available to every profile incl. Research) — a subset of the parent's grants, not widening, but wider than the profile name implies.","original_execution":"journal#24","payload":{"claims":["Child grants are computed as an intersection: authority = Map.filterWithKey same currentGrants where same name contract = Map.lookup name task.grants == Just contract, so a child receives only (name, contract) pairs present in both the caller's currentGrants and the parent task's DB-loaded task.grants with exact string equality per name (source:src/Max/DB/Task/Workflow.hs).","grants = taskGrants request.profile authority then intersects the profile whitelist from source:src/Max/Task/Types.hs (base names incl. task_start, maxops_operations, maxops_query, web_search; Operations adds maxops_execute; Browser adds browser_* and view_zhihu; Sandbox adds sandbox_*, import_file_to_sandbox, nix_search; Research adds nothing), so child grants ⊆ parent task.grants ∩ profile-allowed names; even an inflated caller-supplied currentGrants cannot exceed the parent's recorded grants because the filter anchors on loadTask(task.grants), so agent() cannot widen the parent's authority.","Rejection point 1: any beginAgentStep guard failure — authorizeWithin turn (ExecutionWork CheckOnly) not passing, parent not a live root (isNothing task.parent), or task_start grant missing/mismatched (Map.lookup \"task_start\" currentGrants must equal Map.lookup \"task_start\" task.grants) — falls to the catchall Left \"agent() requires a live root task with delegation authority\" (source:src/Max/DB/Task/Workflow.hs).","Rejection point 2: workflowSteeringPending turn (an unprocessed 'steer' event for the turn's live attempt) → Left \"workflow_steering_pending\" (source:src/Max/DB/Task/Workflow.hs).","Rejection point 3: profile capability ceiling — if the requested profile's required marker grant (browser_navigate / sandbox_exec / maxops_execute; Research requires none) is absent from the computed child grants → Left \"requested profile exceeds the parent capability ceiling\" (source:src/Max/DB/Task/Workflow.hs).","Rejection points 4-5: admitTaskWithin failure → Left (admissionErrorText failure); an invalidated cached child generation → Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\" (source:src/Max/DB/Task/Workflow.hs).","Rejection point 6: pollAgentStep requires authorizeWithin to pass and task.revision == step.childRevision on the loaded child; otherwise Left \"parent or child execution was cancelled, superseded or expired\"; endAgentWait only deletes the workflow_agent_waits row and grants nothing (source:src/Max/DB/Task/Workflow.hs).","No recursive escalation: the delegation gate requires isNothing task.parent, so a child (non-root) task can never call agent() again, and workflowAllowed returns False for a child's turn via NOT EXISTS over workflow_agent_steps joined to that child's task_attempts, restricting children to the normal agent loop and blocking ordinary workflows on child turns (source:src/Max/DB/Task/Workflow.hs).","Limits of the evidence: authorizeWithin (Max.DB.Task.Authorization), admitTaskWithin (Max.DB.Task.Admission), loadTask/loadAttempt (Max.DB.Task.Record) and taskIsLive (Max.Task.State) are imported by the workflow module but not provided, so authorization policy, admission/budget/fencing and liveness internals are unverified (the module header says admission, fencing and budgets remain owned by the task subsystem); grant-name-to-tool enforcement on the child turn and agent()'s tool exposure are outside the two audited files; contract values are opaque Text compared by string equality; the taskGrants base list (incl. maxops_operations/maxops_query for every profile, even Research) is a subset of parent grants, not a widening, but broader than the profile name suggests (source:src/Max/Task/Types.hs)."],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#6","unresolved":["Max.DB.Task.Authorization (authorizeWithin) implementation not provided; authorization policy for ExecutionWork CheckOnly unverified","Max.DB.Task.Admission (admitTaskWithin) not provided; admission/budget/fencing checks applied to the child unverified","Max.DB.Task.Record (loadTask/loadAttempt) and Max.Task.State (taskIsLive) not provided; liveness/supersession semantics inferred only from usage","Enforcement of the child's grant map at tool dispatch on the child turn is not in the audited files","agent() tool exposure/registration to the model is not in the audited files"]},{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"findings":"Audit of the five provided sources. Verdict: (1) Two awaited siblings CANNOT overspend the shared call/round budget; the check-and-increment is atomic under overlapping locks and the counters are reservation-based. (2) Waiting parents CANNOT deadlock task capacity: waiting tasks are excluded from every capacity predicate, every running attempt is time-boxed by a 60s lease with a 40-attempt cap, and deadline expiry makes stuck waiting tasks reclaimable. Residual risks noted are capacity under-enforcement (stale wait rows) and orphaned queued children — not overspend or deadlock — and both depend on Settlement.hs which was not provided.\n\nBudget-side details (Authorization.hs, Record.hs, Admission.hs, ConversationLock.hs):\n- authorizeWithin gates every ExecutionWork on `available reservation task && available reservation budget` (ReserveCall: `calls < maxCalls`; ReserveRound: `rounds < maxRounds`), then applies `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` (same for rounds), updating the child row and the shared root row in the caller's pinned transaction.\n- The budget row is `root <- loadTask (fromMaybe task.taskId task.root)`; admission propagates the parent's root to every child (`rootId = (…fromMaybe task.taskId task.root) <$> parent`), so all siblings share one counter pair. max_calls=200 / max_rounds=400 are set at insertion.\n- Siblings share one conversation: children inherit the caller turn's conversation_id (Admission.hs); claimTask creates the sibling's attempt turn in `task.conversationId` (Scheduling.hs). authorizeWithin first takes `lockTurnConversation turn` = `…FOR UPDATE OF c` on the conversations row (ConversationLock.hs), serializing both siblings' auth transactions; independently, loadTask takes `SELECT … FROM durable_tasks … FOR UPDATE` on child and root rows (Record.hs), so a second sibling re-reads the first sibling's incremented calls_reserved/rounds_reserved after commit. No TOCTOU overspend.\n- TaskRecord.calls/rounds decode from calls_reserved/rounds_reserved — \"Reserved usage, including work still in flight\" (Record.hs) — so budget held by an in-flight or waiting sibling is already inside the shared root counters. The authorization guards also require `task.status == Running`, so a sibling parked in `waiting` cannot increment any counter; it can only spend what it already reserved.\n- Two siblings can hold budget simultaneously only via prior reservations, which are already counted on the shared root row; combined with serialization, the shared 200/400 cap is globally enforced across the sibling set.\n\nCapacity-side details (Scheduling.hs):\n- claimTask takes `pg_advisory_xact_lock(872008)` (shared by all claimers) so the capacity check and reservation commit exactly once per claim; it then takes lockTaskConversation and re-runs `eligibleTask (Just identifier)` under lock, closing the TOCTOU window on eligibility.\n- All three capacity predicates in eligibleTask count only `active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits …)` — caps 40 global, 10 per conversation, 10 per owner. A task in `waiting` status matches none of these (status is not 'running'), and a running task with a live wait row is excluded, so a waiting parent holds ZERO capacity slots and cannot deadlock the queue.\n- No slot is held indefinitely: each attempt gets a 60s lease (`addUTCTime 60 now` into task_attempts.lease_until); the same advisory-locked pass fences expired attempts (execution_journal → 'outcome-unknown', agent_turns → 'crashed', task → 'queued', attempt+1); `task.attempt >= 40` or `deadline <= now` forces terminal `completeTask … BudgetExhausted`. A stuck waiting/retrying task becomes eligible via `work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp()`, so even an un-woken parent is bounded by retry exhaustion rather than deadlock.\n- The parent liveness gate includes 'waiting' (`parent.status IN ('queued','running','waiting','retrying')`), so children of a waiting parent remain claimable and can complete to unblock it.\n- Lock order is uniform: advisory (claim path only) → conversations row (lockTurnConversation/lockTaskConversation) → durable_tasks rows (loadTask FOR UPDATE) → task_attempts row (loadAttempt FOR UPDATE); no circular wait exists between claimers (global advisory lock) or between authorization paths (conversation-first), so the parent/child/wait machinery cannot deadlock on locks.\n\nResidual findings (not overspend, not capacity deadlock):\n- Stale workflow_agent_waits rows would make the capacity counts undercount, permitting over-commit beyond 40/10/10 (opposite direction from deadlock). Wait-row hygiene lives in Max.DB.Task.Settlement, imported by Scheduling.hs but not in the provided file set, so prompt removal on child completion / crash settlement is not verifiable from the given files.\n- A `queued` child whose parent becomes terminal is permanently ineligible (parent gate) with no deadline branch for 'queued' — a zombie-task liveness gap, presumably handled by the missing Settlement cascade; also not verifiable here.\n- ReserveCall and ReserveRound are independent counter pairs, so a task can hold up to 200 calls AND 400 rounds at the root — by design, not overspend; the `task.deadline <= now || budget.deadline <= now` guard further stops all reservations after deadlines.","original_execution":"journal#25","payload":{"claims":["Shared-budget enforcement (Authorization.hs): every ExecutionWork reservation is gated on `available reservation task && available reservation budget` (available ReserveCall = `task.calls < task.maxCalls`, available ReserveRound = `task.rounds < task.maxRounds`), then applied as `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` (or rounds_reserved) updating BOTH the child row and the shared root row inside the caller's pinned transaction; root is `loadTask (fromMaybe task.taskId task.root)` and admission propagates the parent's root to every child, so all siblings share one counter pair (max_calls=200, max_rounds=400 set at insert in Admission.hs).","Serialization of two sibling authors (ConversationLock.hs + Record.hs): both siblings' attempt turns live in the same conversation (children inherit the caller turn's conversation_id in Admission.hs; claimTask creates sibling attempt turns in task.conversationId in Scheduling.hs). authorizeWithin first takes lockTurnConversation = `SELECT c.conversation_id ... FOR UPDATE OF c`, so the two siblings' authorization transactions are serialized on the conversations row; independently loadTask takes `SELECT ... FROM durable_tasks ... FOR UPDATE` on both the child row and the root row, so the second sibling re-reads the first sibling's already-committed calls_reserved/rounds_reserved. The check-and-increment is therefore atomic with respect to the shared root budget: no TOCTOU overspend.","Waiting siblings cannot add spend (Record.hs + Authorization.hs): TaskRecord.calls/rounds decode from calls_reserved/rounds_reserved, documented as 'Reserved usage, including work still in flight', so budget held by an in-flight or waiting sibling is already inside the shared root counters; and the authorization guard requires `task.status == Running` plus matching revision/attempt and a live lease, so a sibling parked in `waiting` cannot increment any counter at all. Two awaited siblings can hold previously reserved budget but cannot exceed the shared root's max_calls/max_rounds.","Claim path is globally serialized and re-validated (Scheduling.hs): claimTask takes `pg_advisory_xact_lock(872008)` shared by all claimers, then lockTaskConversation, then re-runs `eligibleTask (Just identifier)` under lock before starting the attempt, so a candidate changed while waiting for its conversation lock is re-evaluated and the capacity check + reservation commit once.","Waiting parents hold zero capacity and cannot deadlock the queue (Scheduling.hs): all three capacity predicates in eligibleTask count only `active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits ...)` with caps 40 global / 10 per conversation / 10 per owner; a task in `waiting` status is excluded from every count (status is not 'running'), and running tasks with a live workflow_agent_waits row are also excluded. A parent parked in `waiting` therefore consumes no capacity slot.","No capacity slot is held indefinitely (Scheduling.hs): each attempt gets a 60s lease (`addUTCTime 60 now` into task_attempts.lease_until); the advisory-locked pass fences expired attempts (execution_journal -> 'outcome-unknown', agent_turns -> 'crashed', task -> 'queued', attempt+1), and `task.attempt >= 40` or `deadline <= now` forces terminal completeTask with BudgetExhausted. Stuck waiting/retrying tasks become claimable via `work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp()`, so an un-woken parent is bounded by retry exhaustion, not deadlock.","Children of a waiting parent stay eligible (Scheduling.hs): the parent gate is `work.parent_task_id IS NULL OR EXISTS (parent.status IN ('queued','running','waiting','retrying'))`, which includes 'waiting', so a parent parked awaiting its children does not freeze its children, and their completion frees the parent for re-claim.","Lock order is uniform and deadlock-free (ConversationLock.hs, Record.hs, Authorization.hs, Scheduling.hs): advisory lock (claim path only) -> conversations row (lockTurnConversation/lockTaskConversation, FOR UPDATE OF c) -> durable_tasks rows (loadTask FOR UPDATE) -> task_attempts row (loadAttempt FOR UPDATE); all claimers are serialized by the global advisory lock 872008, so no circular wait can form between conversation, task, attempt, or scheduler locks.","Residual risk A (over-commit, not deadlock): a stale workflow_agent_waits row on a live running task makes the capacity predicates under-count, permitting more than 40/10/10 concurrent runs; the wait-row removal/settlement logic lives in Max.DB.Task.Settlement, which is imported by Scheduling.hs but was not provided, so prompt cleanup on child completion or crash is not verifiable from the given files.","Residual risk B (zombies, not deadlock or overspend): a queued child whose parent becomes terminal is permanently ineligible under the Scheduling.hs parent gate (no deadline branch covers status='queued'); cleanup of such orphans presumably sits in the missing Settlement cascade and could not be confirmed from the provided files.","By-design note: ReserveCall and ReserveRound are independent counter pairs (calls_reserved vs rounds_reserved vs max_calls=200 vs max_rounds=400), so a task may hold up to 200 calls AND 400 rounds at the root simultaneously; the deadline guard `task.deadline <= now || budget.deadline <= now -> False` additionally stops all further reservations once either the task's or the shared budget's deadline passes."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#7","unresolved":["Max.DB.Task.Settlement is imported by Scheduling.hs but not provided; cannot verify that workflow_agent_waits rows are removed promptly on child completion or crash (stale rows would under-count capacity, i.e., over-commit, not deadlock).","Cannot verify orphan handling for queued children of terminal parents (permanently ineligible under the Scheduling.hs parent gate); settlement/cascade logic is outside the provided file set.","taskIsLive and task status transition definitions live in Max.Task.State (not provided); assumed to mean non-terminal statuses as used in the ancestor-liveness check."]},{"evidence":["source:src/Max/Task/WorkflowRuntime.hs","source:src/Max/CodeMode/Execution.hs","source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Delegation.hs"],"findings":"Audit of cache reuse, invalidation, and steering boundaries across WorkflowRuntime.hs, CodeMode/Execution.hs, DB/Task/Workflow.hs, and Delegation.hs.\n\nCACHE REUSE\n- Reuse identity: beginAgentStep (DB/Task/Workflow.hs) scopes the workflow_agent_steps lookup by parent_task_id + parent_revision + call_key; call_key = agentCallKey (Delegation.hs) = \"agent:\" + fingerprint({receipts, grants, request}), where request is the full AgentRequest (objective, inputs, profile name, output_contract) and the host's receipts value (WorkflowRuntime.hs) = javaScriptRuntimeVersion + per-skill versions from toolSkillLoads. Grants are folded in as taskGrants(profile, authority) where authority is the intersection of the parent's grants with the current turn's grants.\n- Hit path: if a prior row exists and the child's revision is unchanged and status /= Cancelled, the call re-joins the existing child (no new admission). If the child is still Queued/Running/Retrying it re-registers workflow_agent_waits (markWait) and the runtime keeps polling; if it settled, await immediately returns the DB-cached report as a committed receipt with reused:true and original_execution = journal#COALESCE(settled_journal_id, first_journal_id).\n- What editing a source step reuses: per Delegation.hs's module doc, a source edit creates a new journal step, but \"unchanged semantic calls can still reuse their child in the same parent revision.\" In CodeMode/Execution.hs the source identity (source_fingerprint = sha256(wpEvidence), step_key = sha256({source, args})) is recorded only in boundHooks.ehStart journal-entry metadata; it is NOT part of agentCallKey. So editing the workflow source while the agent() args and receipts are unchanged reuses the previous child task and its settled report — only the new execution's step_key/source_fingerprint change. Only receipt changes (skill package version, JS runtime version), request changes, or grant/profile changes create a new key.\n- Invalidation: (a) child revision changed or child status == Cancelled → admission rejected with \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\"; (b) receipts/grants change → key miss → fresh child via admitTaskWithin under the same key; (c) parent revision change → old rows never match again; (d) profile capability ceiling (browser_navigate/sandbox_exec/maxops_execute must be within parent grants), parent must be a live root task with matching task_start grants; (e) workflowAllowed blocks a child turn from starting another workflow (no nesting).\n\nSTEERING BOUNDARIES\n- Predicate: workflowSteeringPending (DB/Task/Workflow.hs) = a task_events row of kind 'steer' with event_id > GREATEST(work.consumed_event, attempt.seen_event), on the current attempt generation only (attempt.revision = work.revision AND attempt.attempt = work.attempt).\n- Two boundary points: admission (beginAgentStep rejects new agent steps with \"workflow_steering_pending\"; the runtime maps that code through verbatim so CodeMode's steeringOutcome — which matches either the rejected code or a committed {interrupted:true, reason:\"workflow_steering_pending\"} — fires) and polling (WorkflowRuntime's await checks after each empty poll and, if pending, commits the interrupted receipt and stops the 250 ms re-poll).\n- Wasm host stop: on a steering boundary runWasmProgram records the interrupted batch envelopes as cmOutput, finalExit = WasmHostStopped, and codeModeInvocation reports ToolSucceeded (ContinueLoop) — the model sees interrupted receipts as a success value, not a fault.\n\nCANCELLATION AND PROVENANCE GAPS\n1. Infinite poll of dead children: pollAgentStep returns Right Nothing (the \"still running\" path) for a child whose status is terminal (e.g. Cancelled) but has no result report, because the report guard requires Just report. await() then re-polls that dead child every 250 ms with no terminal escape other than a steering event or parent-side authorization failure. A child cancelled without a report can pin a parent wait forever.\n2. Failure conflation: every poll failure (parent or child cancelled, superseded, expired, or lost authorization) collapses to one Left; the runtime throws TaskCancelled for all of them, so expiration or supersession is indistinguishable to the host/model from user cancellation.\n3. Source provenance gap: source_fingerprint/step_key are journal-only metadata and are absent from the reuse key, so two different source steps with identical agent args + receipts share one cache entry. The reuse receipt carries original_execution (parent-side journal) but not the original execution's source fingerprint, and the reuse run's recorded step_key is that of the new source — the step key recorded at reuse does not match the source that produced the data.\n4. original_execution ambiguity: the receipt points at COALESCE(settled_journal_id, first_journal_id) — the parent-side journal that settled or first created the step, not the child's own execution journal; the receipt exposes only the child task id, so per-child provenance depends entirely on the child report's evidence fields.\n5. Stale-generation steering blind spot: workflowSteeringPending matches only the current attempt generation; if the attempt row is stale relative to the work row it returns false, so pending steers are not a boundary — the next poll instead fails authorization and surfaces as TaskCancelled.\n6. Orphaned waits: markWait inserts workflow_agent_waits and endAgentWait deletes only via the runtime's finally; a crash or unclean exit between admission and wait deletion leaves orphan rows, with no cleanup visible in these files.\n7. Steering visibility scope: steering is observed only at agent admission or while polling an agent child; steer events consumed by the parent before a child is admitted (event_id <= both horizons) never interrupt a running child, and no steering boundary applies to plain-tool batches in these files.\n\nCaveats: loadTask/loadAttempt/admitTaskWithin and the worker's cancellation writer are not in the provided files, so items 1, 5, and 6 are inferred from the visible call graph rather than directly observed.","original_execution":"journal#26","payload":{"claims":["Reuse identity: beginAgentStep matches workflow_agent_steps rows by parent_task_id + parent_revision + call_key, where call_key = 'agent:' + fingerprint({receipts, grants, request}) (agentCallKey in Delegation.hs); receipts = JS runtime version + per-skill load versions (WorkflowRuntime.hs), grants = taskGrants(profile, parent grants ∩ current grants).","Editing a source step reuses: a source edit creates only a new journal step (new step_key/source_fingerprint via boundHooks.ehStart in CodeMode/Execution.hs); because source_fingerprint is not part of agentCallKey, an unchanged agent() request + receipts reuses the prior child task and its settled report, per Delegation.hs's module doc.","Cache hit: child revision unchanged and status /= Cancelled re-joins the child; still Queued/Running/Retrying re-registers workflow_agent_waits and keeps polling; settled children return the DB-cached report with a committed receipt carrying reused:true and original_execution = journal#COALESCE(settled_journal_id, first_journal_id).","Invalidation: child revision change or Cancelled status rejects admission with 'cached child generation was invalidated'; receipts (skill package or runtime version), grants, profile, or request changes force a fresh child; a parent revision bump stops old rows from ever matching; workflowAllowed prevents a child turn from running another workflow.","Steering boundary points: (1) admission — beginAgentStep rejects with 'workflow_steering_pending' when an unconsumed steer event (event_id > GREATEST(work.consumed_event, attempt.seen_event) on the current attempt generation) exists; the runtime passes that code through so steeringOutcome fires; (2) polling — WorkflowRuntime's await checks workflowSteeringPending after each empty poll and commits {interrupted:true, reason:'workflow_steering_pending'} to stop the 250 ms re-poll loop.","Wasm host stop on steering: runWasmProgram sets WasmHostStopped, emits the interrupted batch envelopes as the run value, and codeModeInvocation reports ToolSucceeded (ContinueLoop), so steering surfaces to the model as a success value rather than a fault.","Cancellation gap: pollAgentStep returns Right Nothing for a terminal child (e.g. Cancelled) with no result report, which await() treats as 'still running' and re-polls indefinitely (250 ms) until a steering event or parent authorization failure — no terminal outcome exists for report-less cancelled children.","Cancellation gap: all poll failures (cancelled, superseded, expired, lost authorization) collapse to one Left and the runtime throws TaskCancelled for each, making expiration/supersession indistinguishable from user cancellation to host and model.","Provenance gap: source_fingerprint/step_key are journal-entry-only metadata absent from the reuse key, so distinct source steps with identical agent args + receipts share one cache entry; the reuse receipt lacks the original execution's source fingerprint, and the reuse run's step_key reflects the new source, not the source that produced the reused data.","Provenance gap: original_execution points at a parent-side journal (COALESCE(settled_journal_id, first_journal_id)), not the child's own execution journal; the receipt exposes only the child task id, so per-child provenance rests on the child report's evidence fields.","Steering blind spot: workflowSteeringPending matches only the current attempt generation (revision/attempt equality with the work row); a stale attempt row returns false, so pending steers are not a boundary and the next poll instead fails authorization and surfaces as TaskCancelled.","Orphaned waits: markWait inserts workflow_agent_waits and endAgentWait deletes only via the runtime's finally; a crash between admission and wait deletion leaves orphan rows with no cleanup visible in the provided files.","Steering visibility: steering is observed only at agent admission or while polling an agent child; steers consumed by the parent before child admission (event_id <= both horizons) never interrupt a running child, and no steering boundary applies to plain-tool batches in these files."],"sources":["source:src/Max/Task/WorkflowRuntime.hs","source:src/Max/CodeMode/Execution.hs","source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Delegation.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#8","unresolved":["Worker-side cancellation semantics (how a child becomes Cancelled without a report) live outside the four provided files; gap #1's reachability depends on it.","loadTask/loadAttempt/admitTaskWithin implementations are not provided; stale-generation behavior (gap #5) is inferred from the SQL predicates alone.","No cleanup job for orphaned workflow_agent_waits rows is visible in the provided files."]}]},{"actual_models":["qwen3.8-27b"],"children":[{"report":{"evidence":[],"status":"failed","summary":"execution ended without task_finish","unresolved":[]},"status":"failed"},{"report":{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"payload":{"claims":["No overspend of the shared budget by two awaited siblings: authorizeWithin performs check-then-increment inside the caller's pinned transaction and only after lockTurnConversation holds the conversations row lock (ConversationLock.hs: `FOR UPDATE OF c`); the availability check `task.calls < task.maxCalls && budget.calls < budget.maxCalls` (rounds analogous) reads the sibling's and the root budget's `calls_reserved`/`rounds_reserved` under `loadTask`'s `durable_tasks ... FOR UPDATE`, and only then runs `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` for (task, root) (ReserveRound mirrors it). Siblings share one conversations row lock, so sibling B's check always observes sibling A's increment and the root cap cannot be exceeded.","The counters account reservations, not usage: TaskRecord.calls/rounds are documented 'Reserved usage, including work still in flight' and loadTask selects `calls_reserved,rounds_reserved` into `calls,rounds` against `max_calls,max_rounds` (Record.hs). A sibling's in-flight or awaiting reservation is therefore visible to the other sibling's availability check, making the cap a hard reservation cap.","The budget tree is confined to one conversation: admission copies the turn's `conversation_id` into the new task row and inherits `root_task_id` via `fromMaybe task.taskId task.root` from the parent (Admission.hs), so the root budget row and every sibling always sit behind the same conversations row lock; no cross-conversation bypass of the shared budget exists.","Caps are per-kind and fixed at admission: every task is inserted with `max_calls=200, max_rounds=400` (Admission.hs INSERT); ReserveCall checks/increments only the calls counter and ReserveRound only rounds, so headroom cannot be cross-funded between kinds; a root reserving for itself matches one row via `task_id IN (x,x)` and increments exactly once.","Attempt fencing blocks double-reservation through stale turns: authorizeWithin requires `task.revision == execution.revision && task.attempt == execution.attempt` plus `execution.leaseUntil > now`; claimTask's reclaim path fences old attempts (`execution_journal ... state='outcome-unknown', failure_code='task_lease_expired'`), crashes old turns, and only then mints a new attempt, so one task cannot hold two concurrent reservations against the shared budget.","No release path exists in the audited set: none of the five modules decrements `calls_reserved`/`rounds_reserved`, so a reservation orphaned by a crashed step permanently consumes headroom. This is an availability leak (siblings or the task itself fail to reach the true cap), not an overspend past the cap.","A waiting parent consumes zero capacity: all three capacity subqueries in eligibleTask (Scheduling.hs) count only `active.status='running'` tasks joined to a live attempt lease AND `NOT EXISTS(workflow_agent_waits ...)`, against limits bound by the parameters `(40, 10, 10)` — 40 global, 10 per-conversation, 10 per-owner. A parent in 'waiting' status and a running turn with a registered workflow wait are excluded from every counter.","A waiting parent also does not block its children: the parent gate `parent.status IN ('queued','running','waiting','retrying')` includes 'waiting', so children of an awaited parent remain claimable; there is no lock or counter cycle between a waiting parent and the capacity mechanism in the provided code, so no deadlock of task capacity arises from waiting parents.","Capacity occupancy is time-bounded: claimTask mints a 60-second attempt lease (`lease_until = now + addUTCTime 60`); a running task without a live lease is reclaimable via the `status='running' AND NOT EXISTS(live attempt)` clause, and deadline-passed `('waiting','retrying')` tasks are reclaimed and terminated via `completeTask task BudgetExhausted`. Claim sweeps serialize on `pg_advisory_xact_lock(872008)`, which is taken before the task conversation lock, while execution-side flows take the conversation lock first and only per-resource `pg_advisory_xact_lock(hashtextextended('task-resource:'||...))` keys afterwards — no ordering cycle exists in the provided code; the residual cost is that settlement of expired attempts runs inside the claim transaction holding 872008, globally serializing scheduling (contention, not deadlock).","Residual risk (capacity leak): the deadline-based reclaim clause covers only `status IN ('waiting','retrying')`; a 'running' task that keeps its attempt lease fresh past `deadline` is never selected by eligibleTask, so a stuck-but-renewing worker can hold one of the 10 per-conversation or 10 per-owner slots indefinitely (lease refresh mechanics are outside the provided files).","Residual risk (zombie children): if a parent reaches a terminal status, the parent gate permanently excludes its queued children from claiming; Settlement.hs (imported by Scheduling.hs, not provided) decides whether terminal state cascades to descendants. If it does not, zombie children accumulate and can exhaust the per-conversation admission cap (`count >= 160 -> AdmissionQueueFull` in Admission.hs), stalling new admissions for that conversation.","Scope limits: the pre-deadline workflow-wait wake path and settlement are not in the provided files; the no-overspend conclusion covers every reservation path present in them (authorizeWithin is the only counter writer in the audited set) and relies on the documented caller discipline stated in the ConversationLock.hs and Record.hs module headers (caller-owned pinned transaction; conversation lock acquired before work rows)."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"status":"succeeded","summary":"Audit of shared call/round budget and task-capacity mechanics across the five provided modules. VERDICT 1 (overspend): Two awaited siblings in the same conversation CANNOT overspend the shared root call/round budget. Every budget reservation happens in authorizeWithin (Authorization.hs), which runs only after lockTurnConversation takes the conversations row lock (ConversationLock.hs, `FOR UPDATE OF c`) and, inside the caller's pinned transaction, re-checks `available reservation task && available reservation budget` — i.e. `calls < maxCalls` / `rounds < maxRounds` on BOTH the sibling's row and the root budget row — reading the reserved counters via loadTask's `durable_tasks ... FOR UPDATE` projection (Record.hs maps `calls_reserved,rounds_reserved` into `calls,rounds`, documented as \"Reserved usage, including work still in flight\") and only then issues `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (task,root)` (ReserveRound mirrors this for rounds). Because the check-then-increment is atomic under one conversations row lock, and admission guarantees the root and every sibling share that one conversation (Admission.hs copies the turn's conversation_id and inherits root_task_id from the parent), sibling B's availability check always sees sibling A's increment — the cap (200 calls / 400 rounds, fixed at INSERT) cannot be exceeded. Self-budget `IN (x,x)` matches one row and increments once; per-kind counters can't cross-fund. No decrement/release of the reserved counters exists anywhere in the provided files, so lost reservations leak headroom (availability loss), they never push the counter past the cap. VERDICT 2 (deadlock): A waiting parent CANNOT deadlock the capacity counters. All three capacity subqueries in Scheduling.hs's eligibleTask count only `active.status='running'` tasks with a live attempt lease AND `NOT EXISTS(workflow_agent_waits)` against the limits 40 global / 10 per-conversation / 10 per-owner (bound via `(40,10,10)` parameters); a 'waiting' parent and running turns with registered workflow waits are excluded from every counter, and the parent gate `parent.status IN ('queued','running','waiting','retrying')` keeps a waiting parent's children claimable — no lock or counter cycle exists between a waiting parent and the scheduler. Occupancy is time-bounded: claims mint 60s attempt leases (`addUTCTime 60 now`), lease-lapsed running tasks are reclaimable, and deadline-passed waiting/retrying tasks are reclaimed and terminated (`completeTask task BudgetExhausted`); claim sweeps serialize on `pg_advisory_xact_lock(872008)` (taken before the conversation lock, so no ordering cycle with execution-side locks, which take conversation lock first and only per-resource advisory keys afterwards). Residual risks are conditional, not demonstrated deadlocks: (a) a 'running' task that keeps its lease fresh past deadline is never reclaimed (the deadline clause covers only waiting/retrying), so a stuck-renewing worker can pin one of the 10 per-conversation/owner slots indefinitely; (b) children of a terminally failed parent become permanently claim-ineligible under the parent gate, and whether settlement cascades cancellation to descendants is decided in Settlement.hs, which was NOT provided — uncascaded zombies would accumulate against the 160-per-conversation AdmissionQueueFull cap; (c) the pre-deadline wait-wake resume path and settlement itself are outside the provided files, so the no-overspend conclusion covers exactly the reservation paths present (authorizeWithin is the only counter writer in the audited set) plus the documented caller discipline (pinned transaction; conversation lock before work rows).","unresolved":["Settlement.hs is imported by Scheduling.hs but not provided: whether terminal parent state cascades/cancels descendant tasks, and whether wait-wake (workflow wait firing before deadline) resumes a sibling through authorizeWithin, could not be verified.","No decrement/release of calls_reserved/rounds_reserved appears in the provided files; whether settlement elsewhere releases reservations (bounding the headroom-leak risk) is unverified.","Attempt-lease refresh/heartbeat behavior (whether a stuck 'running' worker can keep its 60s lease live indefinitely) is outside the provided files; the eligibleTask deadline reclaim clause covers only 'waiting'/'retrying' statuses.","Definitions of taskIsLive (Max.Task.State), ExecutionStep/StepReservation (Max.Execution.Types), and workflow_agent_waits lifecycle are not in the provided set and were reasoned about from usage only.","The guarantee that all budget reservers hold the conversations lock before work rows depends on caller discipline documented in module headers (ConversationLock.hs, Record.hs); a caller violating the order or splitting check/increment across transactions would break it."]},"status":"succeeded"},{"report":{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"payload":{"claims":["CONCLUSION: On the visible grant-computation path, agent() CANNOT widen the parent's authority. child_grants = taskGrants(profile, authority) with authority = currentGrants ∩_exact task.grants, so child_grants ⊆ task.grants (the parent's grants); the child inherits a strict, exact-contract subset of the parent, not a superset.","INTERSECTION (value-anchored to the parent): in beginAgentStep, `same name contract = Map.lookup name task.grants == Just contract` intersects the caller's currentGrants with the parent's task.grants on both name and exact contract value. Because the lookup is anchored to the PARENT's task.grants, even an over-broad currentGrants cannot lift the child above the parent's capability.","PROFILE FILTER adds nothing: taskGrants (Types.hs) = Map.filterWithKey (\\name _ -> name `elem` allowed); it preserves existing contract values and only permits profile-allowed names (Operations->maxops_execute, Browser->browser_*, Sandbox->sandbox_*, Research->none). A name reaches the child's map only if already present in authority.","CHILD ADMISSION uses the narrowed map: admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants — the child is admitted with the computed (subset) grants, not the parent's full grant set.","REJECTION — delegation/root guard: `Just task | allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants`. Failure falls through to Left: \"workflow_steering_pending\" (if steering pending) else \"agent() requires a live root task with delegation authority\". Enforces turn authorization, no pending steering, a ROOT parent (isNothing task.parent — non-root parents are denied), caller holding task_start, and the caller's task_start contract EXACTLY matching the parent's.","REJECTION — profile ceiling: `required` maps Browser->\"browser_navigate\", Sandbox->\"sandbox_exec\", Operations->\"maxops_execute\", Research->Nothing; absence of the required grant from the computed grants yields Left \"requested profile exceeds the parent capability ceiling\", so a child cannot self-upgrade to a profile the parent cannot support.","REJECTION — cached step & admission: an invalidated prior child generation yields Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\"; an admitTaskWithin failure yields Left (admissionErrorText failure).","EVIDENCE LIMIT: admitTaskWithin (Max.DB.Task.Admission), authorizeWithin (Max.DB.Task.Authorization), agentCallKey (Max.Task.Delegation), and the task.grants derivation (Record/State) are not in the provided files. The no-widening result holds for the visible grant computation but depends on admission preserving the passed `grants` as the child's ceiling and on task.grants faithfully encoding the parent's authority; child runtime enforcement of granted names is not visible here."],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"status":"succeeded","summary":"AUDIT: Can agent() widen its parent's authority? Based on the visible grant-computation path, NO — the child's grants are a strict subset (exact contract match) of the parent's grants, further filtered by the requested profile. Details and evidence limits below.\n\nCHAIN (beginAgentStep, src/Max/DB/Task/Workflow.hs):\n  authority = Map.filterWithKey same currentGrants, where same name contract = (Map.lookup name task.grants == Just contract)\n  grants    = taskGrants request.profile authority\n  admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants\nSo child_grants ⊆ authority ⊆ task.grants (parent). The binding constraint is the PARENT's task.grants (via the exact-match lookup), not the caller's currentGrants.\n\nINTERSECTION POINTS (where authority is narrowed):\n- The `same` predicate (Workflow.hs) intersects the caller's currentGrants with the parent's task.grants on BOTH name and exact contract value. Because it is anchored to `Map.lookup name task.grants`, even an over-broad currentGrants cannot lift the child above the parent.\n- `taskGrants` (Types.hs) = `Map.filterWithKey (\\name _ -> name `elem` allowed)`: a name-filter that PRESERVES existing contract values and ADDS NOTHING. Profile-specific additions (e.g. Operations→maxops_execute, Browser→browser_*, Sandbox→sandbox_*) are only *permitted* names; a name enters the child's map only if it is already in `authority`.\n- `admitTaskWithin ... grants` admits the child with the narrowed map, not the parent's full grant set.\n\nREJECTION POINTS (where agent() is denied):\n- Guard `Just task | allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants`. Failing any clause falls through to Left: \"workflow_steering_pending\" if pending, else \"agent() requires a live root task with delegation authority\". Enforces: turn authorized (authorizeWithin CheckOnly), no steering pending, parent is a ROOT task (isNothing task.parent — non-root parents are rejected), caller holds task_start, and the caller's task_start contract EXACTLY equals the parent's.\n- Profile ceiling: `required` maps Browser→\"browser_navigate\", Sandbox→\"sandbox_exec\", Operations→\"maxops_execute\", Research→Nothing; if the required name is absent from the computed grants → Left \"requested profile exceeds the parent capability ceiling\". A child cannot self-upgrade to a profile the parent cannot support.\n- Cached step reuse: a prior step whose child generation was invalidated → Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\".\n- Admission failure from admitTaskWithin → Left (admissionErrorText failure).\n\nLIMITS OF THE EVIDENCE (what these two files do NOT show):\n- admitTaskWithin (Max.DB.Task.Admission) is not provided. The no-widening conclusion holds for the visible grant computation but DEPENDS on the admission layer honoring the passed `grants` as the child's ceiling (not re-deriving/merging/defaulting a broader set). Unverified here.\n- authorizeWithin (Max.DB.Task.Authorization) not provided; the `allowed` CheckOnly precondition's semantics are opaque (it is a necessary gate, but what it checks is not shown).\n- agentCallKey (Max.Task.Delegation) not provided (used for step idempotency, not authority).\n- The representation/derivation of task.grants (Max.DB.Task.Record / Max.Task.State) not provided. The audit assumes task.grants faithfully encodes the parent's delegated authority; if the parent's stored grants were broader than the parent's true authority, the child would inherit that broader map — a parent-side fidelity issue, not a widening by agent().\n- currentGrants is a caller-supplied parameter; these files do not re-verify it against the turn's live grants. Robustness comes solely from the exact-match filter anchoring to the parent's task.grants.\n- Static source snapshot only; the child's RUNTIME enforcement of its granted names at execution time is not visible in these files.","unresolved":["admitTaskWithin (Max.DB.Task.Admission) not provided — cannot confirm the admission layer preserves the passed `grants` as the child's ceiling rather than widening them.","authorizeWithin (Max.DB.Task.Authorization) not provided — the CheckOnly `allowed` gate's exact semantics are opaque.","agentCallKey (Max.Task.Delegation) and task.grants derivation (Max.DB.Task.Record / Max.Task.State) not provided.","Child's runtime enforcement of granted names at execution time is not visible in the two provided files."]},"status":"succeeded"}],"completed":false,"completion_tokens":38623,"mode":"parallel","model_calls":6,"prompt_tokens":25439,"seconds":205.221701,"source_reads":11,"started_at":"2026-09-12T09:40:19.650983Z","usage_records":6,"workflow_output":[{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"findings":"AUDIT: Can agent() widen its parent's authority? Based on the visible grant-computation path, NO — the child's grants are a strict subset (exact contract match) of the parent's grants, further filtered by the requested profile. Details and evidence limits below.\n\nCHAIN (beginAgentStep, src/Max/DB/Task/Workflow.hs):\n  authority = Map.filterWithKey same currentGrants, where same name contract = (Map.lookup name task.grants == Just contract)\n  grants    = taskGrants request.profile authority\n  admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants\nSo child_grants ⊆ authority ⊆ task.grants (parent). The binding constraint is the PARENT's task.grants (via the exact-match lookup), not the caller's currentGrants.\n\nINTERSECTION POINTS (where authority is narrowed):\n- The `same` predicate (Workflow.hs) intersects the caller's currentGrants with the parent's task.grants on BOTH name and exact contract value. Because it is anchored to `Map.lookup name task.grants`, even an over-broad currentGrants cannot lift the child above the parent.\n- `taskGrants` (Types.hs) = `Map.filterWithKey (\\name _ -> name `elem` allowed)`: a name-filter that PRESERVES existing contract values and ADDS NOTHING. Profile-specific additions (e.g. Operations→maxops_execute, Browser→browser_*, Sandbox→sandbox_*) are only *permitted* names; a name enters the child's map only if it is already in `authority`.\n- `admitTaskWithin ... grants` admits the child with the narrowed map, not the parent's full grant set.\n\nREJECTION POINTS (where agent() is denied):\n- Guard `Just task | allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants`. Failing any clause falls through to Left: \"workflow_steering_pending\" if pending, else \"agent() requires a live root task with delegation authority\". Enforces: turn authorized (authorizeWithin CheckOnly), no steering pending, parent is a ROOT task (isNothing task.parent — non-root parents are rejected), caller holds task_start, and the caller's task_start contract EXACTLY equals the parent's.\n- Profile ceiling: `required` maps Browser→\"browser_navigate\", Sandbox→\"sandbox_exec\", Operations→\"maxops_execute\", Research→Nothing; if the required name is absent from the computed grants → Left \"requested profile exceeds the parent capability ceiling\". A child cannot self-upgrade to a profile the parent cannot support.\n- Cached step reuse: a prior step whose child generation was invalidated → Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\".\n- Admission failure from admitTaskWithin → Left (admissionErrorText failure).\n\nLIMITS OF THE EVIDENCE (what these two files do NOT show):\n- admitTaskWithin (Max.DB.Task.Admission) is not provided. The no-widening conclusion holds for the visible grant computation but DEPENDS on the admission layer honoring the passed `grants` as the child's ceiling (not re-deriving/merging/defaulting a broader set). Unverified here.\n- authorizeWithin (Max.DB.Task.Authorization) not provided; the `allowed` CheckOnly precondition's semantics are opaque (it is a necessary gate, but what it checks is not shown).\n- agentCallKey (Max.Task.Delegation) not provided (used for step idempotency, not authority).\n- The representation/derivation of task.grants (Max.DB.Task.Record / Max.Task.State) not provided. The audit assumes task.grants faithfully encodes the parent's delegated authority; if the parent's stored grants were broader than the parent's true authority, the child would inherit that broader map — a parent-side fidelity issue, not a widening by agent().\n- currentGrants is a caller-supplied parameter; these files do not re-verify it against the turn's live grants. Robustness comes solely from the exact-match filter anchoring to the parent's task.grants.\n- Static source snapshot only; the child's RUNTIME enforcement of its granted names at execution time is not visible in these files.","original_execution":"journal#49","payload":{"claims":["CONCLUSION: On the visible grant-computation path, agent() CANNOT widen the parent's authority. child_grants = taskGrants(profile, authority) with authority = currentGrants ∩_exact task.grants, so child_grants ⊆ task.grants (the parent's grants); the child inherits a strict, exact-contract subset of the parent, not a superset.","INTERSECTION (value-anchored to the parent): in beginAgentStep, `same name contract = Map.lookup name task.grants == Just contract` intersects the caller's currentGrants with the parent's task.grants on both name and exact contract value. Because the lookup is anchored to the PARENT's task.grants, even an over-broad currentGrants cannot lift the child above the parent's capability.","PROFILE FILTER adds nothing: taskGrants (Types.hs) = Map.filterWithKey (\\name _ -> name `elem` allowed); it preserves existing contract values and only permits profile-allowed names (Operations->maxops_execute, Browser->browser_*, Sandbox->sandbox_*, Research->none). A name reaches the child's map only if already present in authority.","CHILD ADMISSION uses the narrowed map: admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants — the child is admitted with the computed (subset) grants, not the parent's full grant set.","REJECTION — delegation/root guard: `Just task | allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants`. Failure falls through to Left: \"workflow_steering_pending\" (if steering pending) else \"agent() requires a live root task with delegation authority\". Enforces turn authorization, no pending steering, a ROOT parent (isNothing task.parent — non-root parents are denied), caller holding task_start, and the caller's task_start contract EXACTLY matching the parent's.","REJECTION — profile ceiling: `required` maps Browser->\"browser_navigate\", Sandbox->\"sandbox_exec\", Operations->\"maxops_execute\", Research->Nothing; absence of the required grant from the computed grants yields Left \"requested profile exceeds the parent capability ceiling\", so a child cannot self-upgrade to a profile the parent cannot support.","REJECTION — cached step & admission: an invalidated prior child generation yields Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\"; an admitTaskWithin failure yields Left (admissionErrorText failure).","EVIDENCE LIMIT: admitTaskWithin (Max.DB.Task.Admission), authorizeWithin (Max.DB.Task.Authorization), agentCallKey (Max.Task.Delegation), and the task.grants derivation (Record/State) are not in the provided files. The no-widening result holds for the visible grant computation but depends on admission preserving the passed `grants` as the child's ceiling and on task.grants faithfully encoding the parent's authority; child runtime enforcement of granted names is not visible here."],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#12","unresolved":["admitTaskWithin (Max.DB.Task.Admission) not provided — cannot confirm the admission layer preserves the passed `grants` as the child's ceiling rather than widening them.","authorizeWithin (Max.DB.Task.Authorization) not provided — the CheckOnly `allowed` gate's exact semantics are opaque.","agentCallKey (Max.Task.Delegation) and task.grants derivation (Max.DB.Task.Record / Max.Task.State) not provided.","Child's runtime enforcement of granted names at execution time is not visible in the two provided files."]},{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"findings":"Audit of shared call/round budget and task-capacity mechanics across the five provided modules. VERDICT 1 (overspend): Two awaited siblings in the same conversation CANNOT overspend the shared root call/round budget. Every budget reservation happens in authorizeWithin (Authorization.hs), which runs only after lockTurnConversation takes the conversations row lock (ConversationLock.hs, `FOR UPDATE OF c`) and, inside the caller's pinned transaction, re-checks `available reservation task && available reservation budget` — i.e. `calls < maxCalls` / `rounds < maxRounds` on BOTH the sibling's row and the root budget row — reading the reserved counters via loadTask's `durable_tasks ... FOR UPDATE` projection (Record.hs maps `calls_reserved,rounds_reserved` into `calls,rounds`, documented as \"Reserved usage, including work still in flight\") and only then issues `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (task,root)` (ReserveRound mirrors this for rounds). Because the check-then-increment is atomic under one conversations row lock, and admission guarantees the root and every sibling share that one conversation (Admission.hs copies the turn's conversation_id and inherits root_task_id from the parent), sibling B's availability check always sees sibling A's increment — the cap (200 calls / 400 rounds, fixed at INSERT) cannot be exceeded. Self-budget `IN (x,x)` matches one row and increments once; per-kind counters can't cross-fund. No decrement/release of the reserved counters exists anywhere in the provided files, so lost reservations leak headroom (availability loss), they never push the counter past the cap. VERDICT 2 (deadlock): A waiting parent CANNOT deadlock the capacity counters. All three capacity subqueries in Scheduling.hs's eligibleTask count only `active.status='running'` tasks with a live attempt lease AND `NOT EXISTS(workflow_agent_waits)` against the limits 40 global / 10 per-conversation / 10 per-owner (bound via `(40,10,10)` parameters); a 'waiting' parent and running turns with registered workflow waits are excluded from every counter, and the parent gate `parent.status IN ('queued','running','waiting','retrying')` keeps a waiting parent's children claimable — no lock or counter cycle exists between a waiting parent and the scheduler. Occupancy is time-bounded: claims mint 60s attempt leases (`addUTCTime 60 now`), lease-lapsed running tasks are reclaimable, and deadline-passed waiting/retrying tasks are reclaimed and terminated (`completeTask task BudgetExhausted`); claim sweeps serialize on `pg_advisory_xact_lock(872008)` (taken before the conversation lock, so no ordering cycle with execution-side locks, which take conversation lock first and only per-resource advisory keys afterwards). Residual risks are conditional, not demonstrated deadlocks: (a) a 'running' task that keeps its lease fresh past deadline is never reclaimed (the deadline clause covers only waiting/retrying), so a stuck-renewing worker can pin one of the 10 per-conversation/owner slots indefinitely; (b) children of a terminally failed parent become permanently claim-ineligible under the parent gate, and whether settlement cascades cancellation to descendants is decided in Settlement.hs, which was NOT provided — uncascaded zombies would accumulate against the 160-per-conversation AdmissionQueueFull cap; (c) the pre-deadline wait-wake resume path and settlement itself are outside the provided files, so the no-overspend conclusion covers exactly the reservation paths present (authorizeWithin is the only counter writer in the audited set) plus the documented caller discipline (pinned transaction; conversation lock before work rows).","original_execution":"journal#48","payload":{"claims":["No overspend of the shared budget by two awaited siblings: authorizeWithin performs check-then-increment inside the caller's pinned transaction and only after lockTurnConversation holds the conversations row lock (ConversationLock.hs: `FOR UPDATE OF c`); the availability check `task.calls < task.maxCalls && budget.calls < budget.maxCalls` (rounds analogous) reads the sibling's and the root budget's `calls_reserved`/`rounds_reserved` under `loadTask`'s `durable_tasks ... FOR UPDATE`, and only then runs `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)` for (task, root) (ReserveRound mirrors it). Siblings share one conversations row lock, so sibling B's check always observes sibling A's increment and the root cap cannot be exceeded.","The counters account reservations, not usage: TaskRecord.calls/rounds are documented 'Reserved usage, including work still in flight' and loadTask selects `calls_reserved,rounds_reserved` into `calls,rounds` against `max_calls,max_rounds` (Record.hs). A sibling's in-flight or awaiting reservation is therefore visible to the other sibling's availability check, making the cap a hard reservation cap.","The budget tree is confined to one conversation: admission copies the turn's `conversation_id` into the new task row and inherits `root_task_id` via `fromMaybe task.taskId task.root` from the parent (Admission.hs), so the root budget row and every sibling always sit behind the same conversations row lock; no cross-conversation bypass of the shared budget exists.","Caps are per-kind and fixed at admission: every task is inserted with `max_calls=200, max_rounds=400` (Admission.hs INSERT); ReserveCall checks/increments only the calls counter and ReserveRound only rounds, so headroom cannot be cross-funded between kinds; a root reserving for itself matches one row via `task_id IN (x,x)` and increments exactly once.","Attempt fencing blocks double-reservation through stale turns: authorizeWithin requires `task.revision == execution.revision && task.attempt == execution.attempt` plus `execution.leaseUntil > now`; claimTask's reclaim path fences old attempts (`execution_journal ... state='outcome-unknown', failure_code='task_lease_expired'`), crashes old turns, and only then mints a new attempt, so one task cannot hold two concurrent reservations against the shared budget.","No release path exists in the audited set: none of the five modules decrements `calls_reserved`/`rounds_reserved`, so a reservation orphaned by a crashed step permanently consumes headroom. This is an availability leak (siblings or the task itself fail to reach the true cap), not an overspend past the cap.","A waiting parent consumes zero capacity: all three capacity subqueries in eligibleTask (Scheduling.hs) count only `active.status='running'` tasks joined to a live attempt lease AND `NOT EXISTS(workflow_agent_waits ...)`, against limits bound by the parameters `(40, 10, 10)` — 40 global, 10 per-conversation, 10 per-owner. A parent in 'waiting' status and a running turn with a registered workflow wait are excluded from every counter.","A waiting parent also does not block its children: the parent gate `parent.status IN ('queued','running','waiting','retrying')` includes 'waiting', so children of an awaited parent remain claimable; there is no lock or counter cycle between a waiting parent and the capacity mechanism in the provided code, so no deadlock of task capacity arises from waiting parents.","Capacity occupancy is time-bounded: claimTask mints a 60-second attempt lease (`lease_until = now + addUTCTime 60`); a running task without a live lease is reclaimable via the `status='running' AND NOT EXISTS(live attempt)` clause, and deadline-passed `('waiting','retrying')` tasks are reclaimed and terminated via `completeTask task BudgetExhausted`. Claim sweeps serialize on `pg_advisory_xact_lock(872008)`, which is taken before the task conversation lock, while execution-side flows take the conversation lock first and only per-resource `pg_advisory_xact_lock(hashtextextended('task-resource:'||...))` keys afterwards — no ordering cycle exists in the provided code; the residual cost is that settlement of expired attempts runs inside the claim transaction holding 872008, globally serializing scheduling (contention, not deadlock).","Residual risk (capacity leak): the deadline-based reclaim clause covers only `status IN ('waiting','retrying')`; a 'running' task that keeps its attempt lease fresh past `deadline` is never selected by eligibleTask, so a stuck-but-renewing worker can hold one of the 10 per-conversation or 10 per-owner slots indefinitely (lease refresh mechanics are outside the provided files).","Residual risk (zombie children): if a parent reaches a terminal status, the parent gate permanently excludes its queued children from claiming; Settlement.hs (imported by Scheduling.hs, not provided) decides whether terminal state cascades to descendants. If it does not, zombie children accumulate and can exhaust the per-conversation admission cap (`count >= 160 -> AdmissionQueueFull` in Admission.hs), stalling new admissions for that conversation.","Scope limits: the pre-deadline workflow-wait wake path and settlement are not in the provided files; the no-overspend conclusion covers every reservation path present in them (authorizeWithin is the only counter writer in the audited set) and relies on the documented caller discipline stated in the ConversationLock.hs and Record.hs module headers (caller-owned pinned transaction; conversation lock acquired before work rows)."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#11","unresolved":["Settlement.hs is imported by Scheduling.hs but not provided: whether terminal parent state cascades/cancels descendant tasks, and whether wait-wake (workflow wait firing before deadline) resumes a sibling through authorizeWithin, could not be verified.","No decrement/release of calls_reserved/rounds_reserved appears in the provided files; whether settlement elsewhere releases reservations (bounding the headroom-leak risk) is unverified.","Attempt-lease refresh/heartbeat behavior (whether a stuck 'running' worker can keep its 60s lease live indefinitely) is outside the provided files; the eligibleTask deadline reclaim clause covers only 'waiting'/'retrying' statuses.","Definitions of taskIsLive (Max.Task.State), ExecutionStep/StepReservation (Max.Execution.Types), and workflow_agent_waits lifecycle are not in the provided set and were reasoned about from usage only.","The guarantee that all budget reservers hold the conversations lock before work rows depends on caller discipline documented in module headers (ConversationLock.hs, Record.hs); a caller violating the order or splitting check/increment across transactions would break it."]},{"evidence":[],"findings":"execution ended without task_finish","original_execution":"journal#47","payload":null,"payload_valid":true,"reused":false,"status":"failed","task":"task#10","unresolved":[]}]},{"actual_models":["qwen3.8-27b"],"children":[{"report":{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"payload":{"claims":["agent() (beginAgentStep, Workflow.hs) cannot widen parent authority within the provided evidence: the child's grants are computed as the intersection of the parent turn's currentGrants and the parent root task's recorded task.grants (name AND contract value must match via `Map.lookup name task.grants == Just contract`), then further filtered by the profile allow-list via taskGrants (Types.hs); no grant present on only one side survives.","Intersection point A (Workflow.hs): `authority = Map.filterWithKey same currentGrants` requires each grant (name+contract) to exist in both the calling turn's current grants and the parent task's recorded grants.","Intersection point B (Types.hs): `taskGrants profile authority` filters to the profile allow-list — base tools (web_search, context_*, memory_list, task_*, etc.) plus profile-specific tools (browser_* for Browser, sandbox_*/nix_search for Sandbox, maxops_execute for Operations) — so the child can only retain grants in that set.","Rejection point (ceiling, Workflow.hs): the profile's required capability (Browser→browser_navigate, Sandbox→sandbox_exec, Operations→maxops_execute; Research→none) must be present in the final intersected grants, otherwise beginAgentStep returns Left \"requested profile exceeds the parent capability ceiling\"; e.g. a parent without maxops_execute cannot delegate to an Operations profile.","Rejection point (guard, Workflow.hs): `allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants` — any failure (turn fails authorizeWithin CheckOnly, caller task missing or not a root, no task_start grant, or task_start contract mismatch vs the parent's recorded grant) yields \"agent() requires a live root task with delegation authority\"; steering-pending yields \"workflow_steering_pending\". The task_start contract-equality check blocks spawning with a delegation grant different from the parent's recorded one.","Depth limit (Workflow.hs): the guard requires the calling task to be a root (isNothing task.parent); a spawned child has a parent, so it is rejected if it calls agent() — delegation is one level deep and children cannot recurse to widen scope; workflowAllowed additionally restricts an awaited child's turn to the normal agent loop.","Child admission (Workflow.hs): `admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants` admits the child under the parent's owner with only the intersected grants, marks a wait row, and records the workflow_agent_steps row; admission failure surfaces as admissionErrorText; the call key includes receipts+grants+request for idempotent caching, and a stale cached generation is rejected with \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\".","pollAgentStep (Workflow.hs) re-checks authorizeWithin turn (ExecutionWork CheckOnly) and that the child's revision matches; without execution authority or on superseded/cancelled state it returns \"parent or child execution was cancelled, superseded or expired\", so a child's report is delivered only while the parent turn retains execution authority.","Types.hs performs no authorization checks itself: taskGrants is a pure Map.filterWithKey over the profile allow-list, and parseProfile/taskHandle are pure parsing; all enforcement visible in the provided files lives in Workflow.hs.","Limit of evidence: only Workflow.hs and Types.hs were provided; authorizeWithin, admitTaskWithin, agentReport, agentCallKey, loadAttempt/loadTask are imported but unread, and currentGrants derivation is not shown — so runtime enforcement of the child's grants on its own tool calls and the admission subsystem's behavior cannot be confirmed from these files."],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"status":"succeeded","summary":"Audit of whether agent() can widen its parent's authority, based on src/Max/DB/Task/Workflow.hs and src/Max/Task/Types.hs. VERDICT: within the evidence, agent() cannot widen parent authority. The child's grant set is the intersection of (a) the calling parent turn's current grants (currentGrants) and (b) the parent root task's recorded task.grants — both grant name and contract value must match (Workflow.hs: `authority = Map.filterWithKey same currentGrants`, `same name contract = Map.lookup name task.grants == Just contract`) — then a second filter `taskGrants request.profile authority` (Types.hs) keeps only the profile allow-list (base tools + profile-specific browser_*/sandbox_*/maxops_execute). No grant that exists on only one side survives, so a child cannot hold more than the parent has. REJECTION POINTS (Workflow.hs): (1) guard `allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants` — failure yields \"agent() requires a live root task with delegation authority\" (or \"workflow_steering_pending\" when steering is pending); this also requires the turn's task_start contract to equal the parent's recorded one, blocking grant-rotation widening, and requires the caller to be a ROOT task, so a spawned child (task.parent set) cannot itself call agent() — delegation depth is one level. (2) Profile ceiling: required tool per profile (Browser→browser_navigate, Sandbox→sandbox_exec, Operations→maxops_execute, Research→none) must be present in the final intersected grants, else \"requested profile exceeds the parent capability ceiling\" — a parent lacking maxops_execute cannot spawn an Operations child. (3) Admission failure via admitTaskWithin surfaces as admissionErrorText; child is admitted with parent's owner and only the intersected grants. (4) Invalidated cached child generation: \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\". (5) pollAgentStep re-checks authorizeWithin(turn, ExecutionWork CheckOnly); without it the step is treated as \"parent or child execution was cancelled, superseded or expired\", so reports are only delivered while the parent turn still has execution authority. INTERSECTION POINTS: the two filters above, plus the task_start contract-equality guard. LIMITS OF EVIDENCE: only two files were provided. authorizeWithin, admitTaskWithin, agentReport, agentCallKey, loadAttempt/loadTask are imported but unread; Types.hs is a pure filter with no enforcement. Hence I cannot verify (a) that the child's recorded grants are enforced on its own tool calls at runtime, (b) that admitTaskWithin records exactly the grants passed without re-scoping, or (c) how currentGrants is derived at call time. The no-widening conclusion is therefore scoped to the grant computation shown in these files. No production deployment or performance claims are made.","unresolved":["Enforcement of the child's recorded task.grants on its own tool calls is not visible in the provided files (no runtime check in Types.hs; child execution loop not provided).","Implementations of authorizeWithin, admitTaskWithin, agentReport, agentCallKey, loadAttempt/loadTask were not provided; whether admission re-scope or widens grants cannot be confirmed.","Derivation of the caller's currentGrants map (parent's grants at call time) is outside the provided files, so grant rotation/reduction semantics are inferred only from the intersection and task_start equality guards."]},"status":"succeeded"},{"report":{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"payload":{"claims":["Verdict (overspend): two awaited siblings cannot atomically overspend the shared root call/round budget. In authorizeWithin (Authorization.hs) the reservation path takes the turn conversation row via lockTurnConversation ('... FOR UPDATE OF c', ConversationLock.hs) — siblings share the parent's conversation and thus serialize — then loadAttempt (FOR UPDATE on task_attempts) and loadTask (SELECT ... FROM durable_tasks WHERE task_id=? FOR UPDATE, Record.hs) for both the task row and the root budget row (root <- loadTask (fromMaybe task.taskId task.root)); the guard available = task.calls < task.maxCalls applied to both task and budget and the increment UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (task,budget) execute in one pinned transaction while those rows are locked, so the second sibling's READ COMMITTED FOR UPDATE re-read observes the first +1 even though the UPDATE re-checks nothing in its WHERE clause.","Counter facts: the counters are calls_reserved/rounds_reserved on durable_tasks, projected as calls/rounds in loadTask (Record.hs, documented 'Reserved usage, including work still in flight; see loadTask's projection'); caps are max_calls=200 and max_rounds=400 hardcoded in the Admission.hs INSERT (200 :: Int, 400 :: Int), and each reservation increments both the reserving task's row and the root's row, so all descendants of a root share the root's 200-call/400-round pool in addition to their own per-task caps.","'Awaited' status affects only the concurrency caps, not the budget caps: eligibleTask (Scheduling.hs) counts only active.status='running' tasks with execution.lease_until>clock_timestamp() and excludes any whose execution turn has a row in workflow_agent_waits, in all three subqueries parameterized (40 :: Int, 10 :: Int, 10 :: Int) for global, per-conversation, and per-owner limits; an awaiting sibling holds no capacity slot and is not executing, so it cannot reserve while awaiting, and re-runs the full authorizeWithin guard under the same locks when it resumes.","Residual risk in part 1 is bounded exhaustion rather than overspend: Scheduling.hs fences expired attempts via UPDATE execution_journal SET state='outcome-unknown' ... failure_code='task_lease_expired', and none of the provided modules decrements calls_reserved/rounds_reserved, so two siblings whose attempts expired while awaiting keep their reservations against the shared root pool until the subtree is settled — the pool can be driven to zero (starving later work) but never above max_calls/max_rounds.","Verdict (capacity deadlock): a waiting parent cannot permanently deadlock task capacity. Only status='running' tasks with a live attempt lease and no workflow_agent_waits row count against the 40/10/10 caps (Scheduling.hs), so a parent in status='waiting' is never counted, and a running parent whose turn awaits drops out of all three counts — it keeps its subtree live (eligibleTask requires parent.status IN ('queued','running','waiting','retrying')) without occupying a slot.","Bounded recovery paths in Scheduling.hs prevent permanent wedges: stalled workers are reclaimed via the 60s attempt lease (INSERT INTO task_attempts ... addUTCTime 60 now; running AND NOT EXISTS(live lease for work.attempt) branch), a wedged waiting/retrying parent is reclaimed via work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp(), and start settles completeTask task BudgetExhausted when task.deadline <= now || task.attempt >= 40; deadlines are finite because Admission.hs sets deadline = min(admissionDeadline, maybe admissionDeadline (.deadline) parent) with admissionDeadline from taskDeadlineSeconds.","Claim atomicity: all claimers serialize on pg_advisory_xact_lock(872008) inside a single withTransaction (claimTask, Scheduling.hs), so the eligibleTask capacity read and the reservation commit (agent_turns insert, status='running', attempt=attempt+1, task_attempts insert) are atomic per claim — two claimers cannot both observe one free slot; the winner additionally re-evaluates eligibleTask (Just identifier) after lockTaskConversation before starting (the 'Re-evaluate eligibility under lock' path in Scheduling.hs).","Lock order is one-way, preventing a claim/execution cycle: execution transactions take conversation row (lockTurnConversation/lockTaskConversation, ConversationLock.hs) then task_attempts row then durable_tasks rows (task row before root row, both via loadTask FOR UPDATE in Record.hs) and never take the 872008 advisory lock, while claimers take 872008 before any conversation row; no transaction locks a child task row after the root row, so sibling authorization transactions cannot form a lock-order cycle on {conversation, child, root}.","What exists instead of a capacity deadlock is stranding: queued children whose parent ends in a terminal status are permanently unclaimable because eligibleTask requires parent.status IN ('queued','running','waiting','retrying'), a permanently 'waiting' parent never consumes a capacity slot and its queued children remain eligible, and a lost sibling-notification wait resolves at the parent's deadline via the waiting/retrying deadline branch settling BudgetExhausted.","Audit limits: Settlement.hs (completeTask/settleTurn), the workflow_agent_waits writer/deleter, and the task_start entry point were not provided, so settlement-time refund of reserved counters, wait-row lifecycle, and whether any execution-side transaction can take pg_advisory_xact_lock(872008) (which would invert claimTask's 872008-before-conversation ordering) could not be verified; no performance benefit or production-deployment claim is made."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"status":"succeeded","summary":"Audit of shared call/round budget and task-capacity deadlock, based on the five provided modules.\n\nVERDICT 1 — Two awaited siblings CANNOT atomically overspend the shared root call/round budget at the reservation edge, but they CAN permanently exhaust it via non-refunded reservations.\n- Counters: `calls_reserved`/`rounds_reserved` on durable_tasks, projected as `calls`/`rounds` in `loadTask` (Record.hs, documented as \"Reserved usage, including work still in flight\"). Caps `max_calls`/`max_rounds` are hardcoded 200/400 per task at admission (Admission.hs INSERT `200 :: Int, 400 :: Int`).\n- Sharing: `root <- loadTask (fromMaybe task.taskId task.root)` (Authorization.hs); every ReserveCall/ReserveRound does `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (task,budget)` — both the reserving task's row and the root's row, so all descendants share the root's 200/400 pool. The IN-list dedupes when a task is its own root.\n- Race-free: `authorizeWithin` first takes the turn's conversation row (`lockTurnConversation`: `... FOR UPDATE OF c`, ConversationLock.hs) — sibling turns share the parent's conversation, so they serialize — then `loadAttempt` (FOR UPDATE on task_attempts) and `loadTask` (FOR UPDATE on durable_tasks) for both the task row and the root budget row. The guard `task.calls < task.maxCalls && budget.calls < budget.maxCalls` and the increment are in one pinned transaction while the root row is row-locked, so a second sibling's READ COMMITTED FOR UPDATE re-read picks up the first `+1`. The blind UPDATE (no re-check in WHERE) is safe solely because of these locks.\n- \"Awaited\" only affects concurrency caps: `eligibleTask`'s three count subqueries (Scheduling.hs, params 40 global / 10 per conversation / 10 per owner) count only `status='running'` tasks with a live lease and exclude any turn present in `workflow_agent_waits`. An awaiting sibling holds no slot and is not executing, hence not reserving; on resume it re-runs the full guard under the same locks.\n- Real residual risk (exhaustion, not overspend): Scheduling.hs fences expired attempts to `state='outcome-unknown'` (`failure_code='task_lease_expired'`) and none of the provided modules decrement the reserved counters, so two siblings whose attempts expired while awaiting keep their reservations against the shared root pool until the subtree is settled; the pool can be driven to zero, never above `max_calls`.\n\nVERDICT 2 — Waiting parents CANNOT permanently deadlock task capacity; worst case is a bounded stall plus stranded (dead) subtrees.\n- Only `status='running'` tasks with a live attempt lease and no `workflow_agent_waits` row count against the 40/10/10 caps. A parent in `status='waiting'` is not counted at all; a running parent whose turn is awaiting drops out of all three counts — it holds subtree liveness (waiting ∈ live set in the parent clause) without holding a slot.\n- Every waiting parent has a finite deadline (`taskDeadlineSeconds` at admission; children inherit `min(admissionDeadline, parent.deadline)`), so the `work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp()` branch of `eligibleTask` reclaims it and `start` settles `completeTask task BudgetExhausted`. Stalled workers are reclaimed by the 60s attempt lease (`addUTCTime 60 now` in the task_attempts insert; `running AND NOT EXISTS(live lease)` branch), attempts are capped (`task.attempt >= 40 -> BudgetExhausted`), so a fully wedged slot set is released in bounded time.\n- All claimers serialize on `pg_advisory_xact_lock(872008)` inside one `withTransaction` (Scheduling.hs), making the capacity read and the reservation commit atomic per claim — two claimers cannot both see \"one slot free\". The winner re-checks `eligibleTask (Just identifier)` after `lockTaskConversation`.\n- Lock order is one-way (conversation row → attempt row → task row → root row; claimers take 872008 before conversation rows; execution transactions never take 872008), so no lock-order cycle between claim and execution paths.\n- Stranding, not deadlock: queued children of a parent that ends non-live are permanently unclaimable (`parent.status IN ('queued','running','waiting','retrying')` requirement) — a dead subtree holding no slot.\n\nUNVERIFIABLE FROM PROVIDED FILES: Settlement.hs (completeTask/settleTurn bodies, whether settlements refund reserved counters), the writer/deleter of `workflow_agent_waits` rows, and the task_start entry point (whether it can invoke claimTask synchronously from an execution transaction, which would invert the 872008-vs-conversation lock order).","unresolved":["Settlement module (completeTask/settleTurn) not provided: cannot confirm whether reserved calls/rounds are ever decremented after terminal settlement or outcome-unknown fencing","workflow_agent_waits row lifecycle (insert/delete) is outside the provided files; the capacity-exclusion claim depends on rows being present exactly while a turn awaits","task_start entry point not provided: cannot rule out an execution-side transaction taking pg_advisory_xact_lock(872008) while holding a conversation row (lock-order inversion with claimTask)"]},"status":"succeeded"},{"report":{"evidence":[],"status":"failed","summary":"execution ended without task_finish","unresolved":[]},"status":"failed"}],"completed":false,"completion_tokens":36381,"mode":"serial","model_calls":6,"prompt_tokens":25529,"seconds":282.438772,"source_reads":11,"started_at":"2026-09-12T09:43:44.892333Z","usage_records":6,"workflow_output":[{"evidence":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"],"findings":"Audit of whether agent() can widen its parent's authority, based on src/Max/DB/Task/Workflow.hs and src/Max/Task/Types.hs. VERDICT: within the evidence, agent() cannot widen parent authority. The child's grant set is the intersection of (a) the calling parent turn's current grants (currentGrants) and (b) the parent root task's recorded task.grants — both grant name and contract value must match (Workflow.hs: `authority = Map.filterWithKey same currentGrants`, `same name contract = Map.lookup name task.grants == Just contract`) — then a second filter `taskGrants request.profile authority` (Types.hs) keeps only the profile allow-list (base tools + profile-specific browser_*/sandbox_*/maxops_execute). No grant that exists on only one side survives, so a child cannot hold more than the parent has. REJECTION POINTS (Workflow.hs): (1) guard `allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants` — failure yields \"agent() requires a live root task with delegation authority\" (or \"workflow_steering_pending\" when steering is pending); this also requires the turn's task_start contract to equal the parent's recorded one, blocking grant-rotation widening, and requires the caller to be a ROOT task, so a spawned child (task.parent set) cannot itself call agent() — delegation depth is one level. (2) Profile ceiling: required tool per profile (Browser→browser_navigate, Sandbox→sandbox_exec, Operations→maxops_execute, Research→none) must be present in the final intersected grants, else \"requested profile exceeds the parent capability ceiling\" — a parent lacking maxops_execute cannot spawn an Operations child. (3) Admission failure via admitTaskWithin surfaces as admissionErrorText; child is admitted with parent's owner and only the intersected grants. (4) Invalidated cached child generation: \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\". (5) pollAgentStep re-checks authorizeWithin(turn, ExecutionWork CheckOnly); without it the step is treated as \"parent or child execution was cancelled, superseded or expired\", so reports are only delivered while the parent turn still has execution authority. INTERSECTION POINTS: the two filters above, plus the task_start contract-equality guard. LIMITS OF EVIDENCE: only two files were provided. authorizeWithin, admitTaskWithin, agentReport, agentCallKey, loadAttempt/loadTask are imported but unread; Types.hs is a pure filter with no enforcement. Hence I cannot verify (a) that the child's recorded grants are enforced on its own tool calls at runtime, (b) that admitTaskWithin records exactly the grants passed without re-scoping, or (c) how currentGrants is derived at call time. The no-widening conclusion is therefore scoped to the grant computation shown in these files. No production deployment or performance claims are made.","original_execution":"journal#66","payload":{"claims":["agent() (beginAgentStep, Workflow.hs) cannot widen parent authority within the provided evidence: the child's grants are computed as the intersection of the parent turn's currentGrants and the parent root task's recorded task.grants (name AND contract value must match via `Map.lookup name task.grants == Just contract`), then further filtered by the profile allow-list via taskGrants (Types.hs); no grant present on only one side survives.","Intersection point A (Workflow.hs): `authority = Map.filterWithKey same currentGrants` requires each grant (name+contract) to exist in both the calling turn's current grants and the parent task's recorded grants.","Intersection point B (Types.hs): `taskGrants profile authority` filters to the profile allow-list — base tools (web_search, context_*, memory_list, task_*, etc.) plus profile-specific tools (browser_* for Browser, sandbox_*/nix_search for Sandbox, maxops_execute for Operations) — so the child can only retain grants in that set.","Rejection point (ceiling, Workflow.hs): the profile's required capability (Browser→browser_navigate, Sandbox→sandbox_exec, Operations→maxops_execute; Research→none) must be present in the final intersected grants, otherwise beginAgentStep returns Left \"requested profile exceeds the parent capability ceiling\"; e.g. a parent without maxops_execute cannot delegate to an Operations profile.","Rejection point (guard, Workflow.hs): `allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants` — any failure (turn fails authorizeWithin CheckOnly, caller task missing or not a root, no task_start grant, or task_start contract mismatch vs the parent's recorded grant) yields \"agent() requires a live root task with delegation authority\"; steering-pending yields \"workflow_steering_pending\". The task_start contract-equality check blocks spawning with a delegation grant different from the parent's recorded one.","Depth limit (Workflow.hs): the guard requires the calling task to be a root (isNothing task.parent); a spawned child has a parent, so it is rejected if it calls agent() — delegation is one level deep and children cannot recurse to widen scope; workflowAllowed additionally restricts an awaited child's turn to the normal agent loop.","Child admission (Workflow.hs): `admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants` admits the child under the parent's owner with only the intersected grants, marks a wait row, and records the workflow_agent_steps row; admission failure surfaces as admissionErrorText; the call key includes receipts+grants+request for idempotent caching, and a stale cached generation is rejected with \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\".","pollAgentStep (Workflow.hs) re-checks authorizeWithin turn (ExecutionWork CheckOnly) and that the child's revision matches; without execution authority or on superseded/cancelled state it returns \"parent or child execution was cancelled, superseded or expired\", so a child's report is delivered only while the parent turn retains execution authority.","Types.hs performs no authorization checks itself: taskGrants is a pure Map.filterWithKey over the profile allow-list, and parseProfile/taskHandle are pure parsing; all enforcement visible in the provided files lives in Workflow.hs.","Limit of evidence: only Workflow.hs and Types.hs were provided; authorizeWithin, admitTaskWithin, agentReport, agentCallKey, loadAttempt/loadTask are imported but unread, and currentGrants derivation is not shown — so runtime enforcement of the child's grants on its own tool calls and the admission subsystem's behavior cannot be confirmed from these files."],"sources":["source:src/Max/DB/Task/Workflow.hs","source:src/Max/Task/Types.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#14","unresolved":["Enforcement of the child's recorded task.grants on its own tool calls is not visible in the provided files (no runtime check in Types.hs; child execution loop not provided).","Implementations of authorizeWithin, admitTaskWithin, agentReport, agentCallKey, loadAttempt/loadTask were not provided; whether admission re-scope or widens grants cannot be confirmed.","Derivation of the caller's currentGrants map (parent's grants at call time) is outside the provided files, so grant rotation/reduction semantics are inferred only from the intersection and task_start equality guards."]},{"evidence":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"],"findings":"Audit of shared call/round budget and task-capacity deadlock, based on the five provided modules.\n\nVERDICT 1 — Two awaited siblings CANNOT atomically overspend the shared root call/round budget at the reservation edge, but they CAN permanently exhaust it via non-refunded reservations.\n- Counters: `calls_reserved`/`rounds_reserved` on durable_tasks, projected as `calls`/`rounds` in `loadTask` (Record.hs, documented as \"Reserved usage, including work still in flight\"). Caps `max_calls`/`max_rounds` are hardcoded 200/400 per task at admission (Admission.hs INSERT `200 :: Int, 400 :: Int`).\n- Sharing: `root <- loadTask (fromMaybe task.taskId task.root)` (Authorization.hs); every ReserveCall/ReserveRound does `UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (task,budget)` — both the reserving task's row and the root's row, so all descendants share the root's 200/400 pool. The IN-list dedupes when a task is its own root.\n- Race-free: `authorizeWithin` first takes the turn's conversation row (`lockTurnConversation`: `... FOR UPDATE OF c`, ConversationLock.hs) — sibling turns share the parent's conversation, so they serialize — then `loadAttempt` (FOR UPDATE on task_attempts) and `loadTask` (FOR UPDATE on durable_tasks) for both the task row and the root budget row. The guard `task.calls < task.maxCalls && budget.calls < budget.maxCalls` and the increment are in one pinned transaction while the root row is row-locked, so a second sibling's READ COMMITTED FOR UPDATE re-read picks up the first `+1`. The blind UPDATE (no re-check in WHERE) is safe solely because of these locks.\n- \"Awaited\" only affects concurrency caps: `eligibleTask`'s three count subqueries (Scheduling.hs, params 40 global / 10 per conversation / 10 per owner) count only `status='running'` tasks with a live lease and exclude any turn present in `workflow_agent_waits`. An awaiting sibling holds no slot and is not executing, hence not reserving; on resume it re-runs the full guard under the same locks.\n- Real residual risk (exhaustion, not overspend): Scheduling.hs fences expired attempts to `state='outcome-unknown'` (`failure_code='task_lease_expired'`) and none of the provided modules decrement the reserved counters, so two siblings whose attempts expired while awaiting keep their reservations against the shared root pool until the subtree is settled; the pool can be driven to zero, never above `max_calls`.\n\nVERDICT 2 — Waiting parents CANNOT permanently deadlock task capacity; worst case is a bounded stall plus stranded (dead) subtrees.\n- Only `status='running'` tasks with a live attempt lease and no `workflow_agent_waits` row count against the 40/10/10 caps. A parent in `status='waiting'` is not counted at all; a running parent whose turn is awaiting drops out of all three counts — it holds subtree liveness (waiting ∈ live set in the parent clause) without holding a slot.\n- Every waiting parent has a finite deadline (`taskDeadlineSeconds` at admission; children inherit `min(admissionDeadline, parent.deadline)`), so the `work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp()` branch of `eligibleTask` reclaims it and `start` settles `completeTask task BudgetExhausted`. Stalled workers are reclaimed by the 60s attempt lease (`addUTCTime 60 now` in the task_attempts insert; `running AND NOT EXISTS(live lease)` branch), attempts are capped (`task.attempt >= 40 -> BudgetExhausted`), so a fully wedged slot set is released in bounded time.\n- All claimers serialize on `pg_advisory_xact_lock(872008)` inside one `withTransaction` (Scheduling.hs), making the capacity read and the reservation commit atomic per claim — two claimers cannot both see \"one slot free\". The winner re-checks `eligibleTask (Just identifier)` after `lockTaskConversation`.\n- Lock order is one-way (conversation row → attempt row → task row → root row; claimers take 872008 before conversation rows; execution transactions never take 872008), so no lock-order cycle between claim and execution paths.\n- Stranding, not deadlock: queued children of a parent that ends non-live are permanently unclaimable (`parent.status IN ('queued','running','waiting','retrying')` requirement) — a dead subtree holding no slot.\n\nUNVERIFIABLE FROM PROVIDED FILES: Settlement.hs (completeTask/settleTurn bodies, whether settlements refund reserved counters), the writer/deleter of `workflow_agent_waits` rows, and the task_start entry point (whether it can invoke claimTask synchronously from an execution transaction, which would invert the 872008-vs-conversation lock order).","original_execution":"journal#71","payload":{"claims":["Verdict (overspend): two awaited siblings cannot atomically overspend the shared root call/round budget. In authorizeWithin (Authorization.hs) the reservation path takes the turn conversation row via lockTurnConversation ('... FOR UPDATE OF c', ConversationLock.hs) — siblings share the parent's conversation and thus serialize — then loadAttempt (FOR UPDATE on task_attempts) and loadTask (SELECT ... FROM durable_tasks WHERE task_id=? FOR UPDATE, Record.hs) for both the task row and the root budget row (root <- loadTask (fromMaybe task.taskId task.root)); the guard available = task.calls < task.maxCalls applied to both task and budget and the increment UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (task,budget) execute in one pinned transaction while those rows are locked, so the second sibling's READ COMMITTED FOR UPDATE re-read observes the first +1 even though the UPDATE re-checks nothing in its WHERE clause.","Counter facts: the counters are calls_reserved/rounds_reserved on durable_tasks, projected as calls/rounds in loadTask (Record.hs, documented 'Reserved usage, including work still in flight; see loadTask's projection'); caps are max_calls=200 and max_rounds=400 hardcoded in the Admission.hs INSERT (200 :: Int, 400 :: Int), and each reservation increments both the reserving task's row and the root's row, so all descendants of a root share the root's 200-call/400-round pool in addition to their own per-task caps.","'Awaited' status affects only the concurrency caps, not the budget caps: eligibleTask (Scheduling.hs) counts only active.status='running' tasks with execution.lease_until>clock_timestamp() and excludes any whose execution turn has a row in workflow_agent_waits, in all three subqueries parameterized (40 :: Int, 10 :: Int, 10 :: Int) for global, per-conversation, and per-owner limits; an awaiting sibling holds no capacity slot and is not executing, so it cannot reserve while awaiting, and re-runs the full authorizeWithin guard under the same locks when it resumes.","Residual risk in part 1 is bounded exhaustion rather than overspend: Scheduling.hs fences expired attempts via UPDATE execution_journal SET state='outcome-unknown' ... failure_code='task_lease_expired', and none of the provided modules decrements calls_reserved/rounds_reserved, so two siblings whose attempts expired while awaiting keep their reservations against the shared root pool until the subtree is settled — the pool can be driven to zero (starving later work) but never above max_calls/max_rounds.","Verdict (capacity deadlock): a waiting parent cannot permanently deadlock task capacity. Only status='running' tasks with a live attempt lease and no workflow_agent_waits row count against the 40/10/10 caps (Scheduling.hs), so a parent in status='waiting' is never counted, and a running parent whose turn awaits drops out of all three counts — it keeps its subtree live (eligibleTask requires parent.status IN ('queued','running','waiting','retrying')) without occupying a slot.","Bounded recovery paths in Scheduling.hs prevent permanent wedges: stalled workers are reclaimed via the 60s attempt lease (INSERT INTO task_attempts ... addUTCTime 60 now; running AND NOT EXISTS(live lease for work.attempt) branch), a wedged waiting/retrying parent is reclaimed via work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp(), and start settles completeTask task BudgetExhausted when task.deadline <= now || task.attempt >= 40; deadlines are finite because Admission.hs sets deadline = min(admissionDeadline, maybe admissionDeadline (.deadline) parent) with admissionDeadline from taskDeadlineSeconds.","Claim atomicity: all claimers serialize on pg_advisory_xact_lock(872008) inside a single withTransaction (claimTask, Scheduling.hs), so the eligibleTask capacity read and the reservation commit (agent_turns insert, status='running', attempt=attempt+1, task_attempts insert) are atomic per claim — two claimers cannot both observe one free slot; the winner additionally re-evaluates eligibleTask (Just identifier) after lockTaskConversation before starting (the 'Re-evaluate eligibility under lock' path in Scheduling.hs).","Lock order is one-way, preventing a claim/execution cycle: execution transactions take conversation row (lockTurnConversation/lockTaskConversation, ConversationLock.hs) then task_attempts row then durable_tasks rows (task row before root row, both via loadTask FOR UPDATE in Record.hs) and never take the 872008 advisory lock, while claimers take 872008 before any conversation row; no transaction locks a child task row after the root row, so sibling authorization transactions cannot form a lock-order cycle on {conversation, child, root}.","What exists instead of a capacity deadlock is stranding: queued children whose parent ends in a terminal status are permanently unclaimable because eligibleTask requires parent.status IN ('queued','running','waiting','retrying'), a permanently 'waiting' parent never consumes a capacity slot and its queued children remain eligible, and a lost sibling-notification wait resolves at the parent's deadline via the waiting/retrying deadline branch settling BudgetExhausted.","Audit limits: Settlement.hs (completeTask/settleTurn), the workflow_agent_waits writer/deleter, and the task_start entry point were not provided, so settlement-time refund of reserved counters, wait-row lifecycle, and whether any execution-side transaction can take pg_advisory_xact_lock(872008) (which would invert claimTask's 872008-before-conversation ordering) could not be verified; no performance benefit or production-deployment claim is made."],"sources":["source:src/Max/DB/Task/Authorization.hs","source:src/Max/DB/Task/Scheduling.hs","source:src/Max/DB/Task/Record.hs","source:src/Max/DB/Task/Admission.hs","source:src/Max/DB/ConversationLock.hs"]},"payload_valid":true,"reused":false,"status":"succeeded","task":"task#15","unresolved":["Settlement module (completeTask/settleTurn) not provided: cannot confirm whether reserved calls/rounds are ever decremented after terminal settlement or outcome-unknown fencing","workflow_agent_waits row lifecycle (insert/delete) is outside the provided files; the capacity-exclusion claim depends on rows being present exactly while a turn awaits","task_start entry point not provided: cannot rule out an execution-side transaction taking pg_advisory_xact_lock(872008) while holding a conversation row (lock-order inversion with claimTask)"]},{"evidence":[],"findings":"execution ended without task_finish","original_execution":"journal#78","payload":null,"payload_valid":true,"reused":false,"status":"failed","task":"task#16","unresolved":[]}]}],"complete":true,"controlled_read_tools":true,"deadline_seconds":300,"historical_load_reproduced":false,"parent_orchestration":"host_authored_scripts","profile":"qwen3.8-27b","runtime":"c58f58febefe46a9f154e46ff6f50e9de8cbded688433c1fc5a4e19dc6f59e74","source_hashes":{"src/Max/CodeMode/Execution.hs":"1eb1e729cccd9fc55d76d041166deef3e7b96ef5d8e2d1b84d3e3c9de3c1e607","src/Max/DB/ConversationLock.hs":"cbc90f7bd69460575cee0c11cba7b9b8e2b8baa4f25896e6262e6bee837b750b","src/Max/DB/Task/Admission.hs":"3156d9b8438a065eb3e50aefc7826f8fd8179a209b0f1a03a4b316ee2a328676","src/Max/DB/Task/Authorization.hs":"4b848157bd5f16298b1586c281c2e0c530ae3711f972418c5279a1ee4ce8e05f","src/Max/DB/Task/Record.hs":"de83fdc5c44679a6c06e596e948f91e0e2e40003ab433db57bc68893fc883654","src/Max/DB/Task/Scheduling.hs":"a0de3a8efc391e85b1930be76bd93a2b13114f5e95d0e7ec79329500fd223c68","src/Max/DB/Task/Workflow.hs":"5a0f59b5474c7c4b740b84b5dbe0877afdda0daa9a303494060ca8586a8454b7","src/Max/Task/Delegation.hs":"16040b3a3de0eaea614c5f1a9513194ec8c3844933f8a3883afd52005ff0cddb","src/Max/Task/Types.hs":"3e948cd3568c6bcfb861b1d8f1ee5d662dfd974c26523a886afe2597ea2edf26","src/Max/Task/WorkflowRuntime.hs":"ee94f8f1948aeaeff38b08189784fc3a2fdc17c4a1082f654dbd1203e495c0fe"},"source_snapshot":{"src/Max/CodeMode/Execution.hs":"-- | Wasm adapter for the same host executor used by native model calls.\nmodule Max.CodeMode.Execution\n  ( CodeModeResult (..),\n    CodeModeCall (..),\n    WasmProgram (..),\n    runWasmTools,\n    runWasmProgram,\n    codeModeInvocation,\n  )\nwhere\n\nimport Control.Applicative ((<|>))\nimport Control.Concurrent.STM (atomically, modifyTVar', newTVarIO, readTVarIO, writeTVar)\nimport Crypto.Hash.SHA256 qualified as SHA256\nimport Data.Aeson (Value (..), eitherDecodeStrict', encode, object, toJSON, (.=))\nimport Data.Aeson.KeyMap qualified as KeyMap\nimport Data.Aeson.Types (parseJSON, parseMaybe)\nimport Data.ByteString (ByteString)\nimport Data.ByteString.Base16 qualified as Base16\nimport Data.ByteString.Lazy qualified as LBS\nimport Data.Either (fromRight)\nimport Data.Foldable (toList)\nimport Data.Map.Strict qualified as Map\nimport Data.Maybe (fromMaybe, isJust)\nimport Data.Set qualified as Set\nimport Data.Text (Text)\nimport Data.Text qualified as T\nimport Data.Text.Encoding qualified as TE\nimport Effectful\nimport Effectful.Concurrent (Concurrent)\nimport Effectful.Exception (mask)\nimport Max.CodeMode.Wasm\nimport Max.Effects.Tools (Tools)\nimport Max.Execution.Tools\nimport Max.Execution.Types\nimport Max.Execution.Workflow\nimport Max.Skill.Contract (validateValue)\nimport Max.Tool.Control (LoopControl (..), controlReply, mergeControls)\nimport Max.Tool.Types\n\n-- | Bounded receipts; full leaf results remain in the journal/artifact store.\ndata CodeModeCall = CodeModeCall\n  { ccLabel :: !Text,\n    ccTool :: !Text,\n    ccOutcome :: !Text\n  }\n  deriving stock (Show, Eq)\n\ndata CodeModeResult = CodeModeResult\n  { cmExit :: !WasmExit,\n    cmCalls :: ![CodeModeCall],\n    cmControl :: !LoopControl,\n    cmOutput :: !(Maybe Value),\n    cmSubmittedCalls :: !Int,\n    cmOverBudget :: !Bool,\n    cmRunRef :: !Text,\n    cmWorkflow :: !(Maybe Value)\n  }\n  deriving stock (Show, Eq)\n\n-- | Host-authored submission, not a decoder for model-provided module bytes.\ndata WasmProgram = WasmProgram\n  { wpModule :: !ByteString,\n    wpInput :: !(Maybe ByteString),\n    wpEvidence :: !Value,\n    wpOutputContract :: !(Maybe Value),\n    wpWorkflow :: !(Maybe Value)\n  }\n\nrunWasmTools :: (Tools :> es, Concurrent :> es, IOE :> es) => ExecutionSession -> ExecutionHooks es -> [CatalogTool] -> WasmLimits -> ByteString -> Eff es CodeModeResult\nrunWasmTools session hooks catalog limits binary = runWasmProgram session hooks catalog limits (WasmProgram binary Nothing Null Nothing Nothing)\n\n-- | Orchestration enters outside the leaf gate. Registering this as a leaf Tool\n-- runner would recursively acquire that gate and deadlock.\nrunWasmProgram :: (Tools :> es, Concurrent :> es, IOE :> es) => ExecutionSession -> ExecutionHooks es -> [CatalogTool] -> WasmLimits -> WasmProgram -> Eff es CodeModeResult\nrunWasmProgram session hooks catalog limits program = do\n  label <- freshExecutionLabel session \"wasm\"\n  receipts <- liftIO (newTVarIO [])\n  decisions <- liftIO (newTVarIO ContinueLoop)\n  exhausted <- liftIO (newTVarIO False)\n  submitted <- liftIO (newTVarIO 0)\n  -- One bounded reply buffer, scoped to this run, replaced by the next dispatch.\n  -- Paging this buffer never invokes a tool or reserves another leaf call.\n  replyBuffer <- liftIO (newTVarIO Nothing)\n  interrupted <- liftIO (newTVarIO Nothing)\n  let start =\n        JournalStart\n          label\n          \"host:wasm/v1\"\n          1\n          \"max-wasm-abi-v1\"\n          (object [\"sha256\" .= digest program.wpModule, \"input_sha256\" .= fmap digest program.wpInput, \"program\" .= program.wpEvidence, \"fuel\" .= limits.wlFuel, \"memory_bytes\" .= limits.wlMemoryBytes, \"timeout_micros\" .= limits.wlTimeoutMicros, \"host_calls\" .= limits.wlHostCalls])\n          (toJSON ([] :: [Value]))\n          \"unsafe\"\n      reply value = do\n        liftIO . atomically $ writeTVar replyBuffer Nothing\n        let bytes = LBS.take (4 * 1024 * 1024 + 1) (encode value)\n        if LBS.length bytes > 4 * 1024 * 1024\n          then pure . Just . wire $ object [\"bridge_error\" .= (\"tool result exceeds 4 MiB; calls already ran, do not replay the program\" :: Text)]\n          else\n            if LBS.length bytes <= 65536\n              then pure (Just (LBS.toStrict bytes))\n              else do\n                ref <- freshExecutionLabel session (label <> \"/result\")\n                let body = TE.decodeUtf8 (LBS.toStrict bytes)\n                liftIO . atomically $ writeTVar replyBuffer (Just (ref, body))\n                pure . Just . wire $ object [\"result_ref\" .= ref]\n      dispatch bytes = case parseRequest bytes of\n        Left detail -> pure . Just . wire $ outcomeEnvelope (ToolRejected (ToolFault \"invalid_guest_request\" detail RetrySafe))\n        Right (ReadResult ref offset) -> do\n          buffered <- liftIO (readTVarIO replyBuffer)\n          pure . Just . wire $ case buffered of\n            Just (current, body)\n              | current == ref && offset >= 0 && offset <= T.length body ->\n                  let chunk = T.take 8000 (T.drop offset body)\n                      next = offset + T.length chunk\n                   in object [\"chunk\" .= chunk, \"next\" .= next, \"done\" .= (next == T.length body)]\n            _ -> object [\"bridge_error\" .= (\"result reference or offset is not valid in this run\" :: Text)]\n        Right (Invoke many calls) -> mask $ \\restore -> do\n          used <- liftIO (readTVarIO submitted)\n          let refuse = outcomeEnvelope (ToolRejected (ToolFault \"guest_call_limit\" \"program leaf call limit exceeded\" RetrySafe))\n          if used + length calls > limits.wlHostCalls\n            then reply (if many then toJSON (map (const refuse) calls) else refuse)\n            else invoke restore many calls\n      invoke restore many calls = do\n        requests <- traverse (\\(name, args) -> (\\call -> ToolRequest call name args) <$> freshExecutionLabel session (label <> \"/call\")) calls\n        liftIO . atomically $ modifyTVar' submitted (+ length requests)\n        let hasHost = any ((`elem` [agentName, phaseName]) . (.trName)) requests\n            sourceFingerprint = digest (wire program.wpEvidence)\n            boundHooks = hooks {ehStart = \\step entry -> hooks.ehStart step (if entry.jsToolRef == agentName then entry {jsInput = object [\"args\" .= entry.jsInput, \"source_fingerprint\" .= sourceFingerprint, \"step_key\" .= digest (wire (object [\"source\" .= sourceFingerprint, \"args\" .= entry.jsInput])), \"workflow\" .= program.wpWorkflow]} else entry)}\n            missing = pure (ToolInvocation (ToolRejected (ToolFault \"agent_requires_durable_task\" \"agent and phase require a durable task host\" RetrySafe)) ContinueLoop)\n            handlers =\n              Map.fromList\n                [ (agentName, \\row args -> if any ((== ToolRef \"task_start\") . (.ctDefinition.tdRef)) catalog then maybe missing (\\host -> host.whAgent args row) hooks.ehWorkflow else missing),\n                  (phaseName, \\_ args -> case args of String summary | any ((== ToolRef \"task_progress\") . (.ctDefinition.tdRef)) catalog -> maybe missing (\\host -> host.whPhase summary) hooks.ehWorkflow; _ -> missing)\n                ]\n        batch <- restore (if hasHost then executeHostBatch (maybe False (.whParallel) hooks.ehWorkflow && all ((== agentName) . (.trName)) requests) handlers session boundHooks (catalog <> hostCatalog) requests else executeToolBatch session hooks catalog requests)\n        liftIO . atomically $ do\n          modifyTVar' receipts (reverse [CodeModeCall req.trCallId req.trName (outcomeName invocation.tiOutcome) | (req, invocation) <- zip requests batch.tbInvocations] <>)\n          modifyTVar' decisions (\\previous -> mergeControls (previous : map (.tiControl) batch.tbInvocations))\n          modifyTVar' exhausted (|| batch.tbOverBudget)\n        let boundary = any (\\(request, invocation) -> request.trName == agentName && steeringOutcome invocation.tiOutcome) (zip requests batch.tbInvocations)\n        if boundary\n          then do\n            liftIO . atomically $ writeTVar interrupted (Just (toJSON (map (outcomeEnvelope . (.tiOutcome)) batch.tbInvocations)))\n            pure Nothing\n          else\n            if any (isJust . controlReply . (.tiControl)) batch.tbInvocations\n              then pure Nothing\n              else case (many, batch.tbInvocations) of\n                (False, [invocation]) -> reply (outcomeEnvelope invocation.tiOutcome)\n                (True, invocations) -> reply (toJSON (map (outcomeEnvelope . (.tiOutcome)) invocations))\n                _ -> error \"executeToolBatch violated result cardinality\"\n  (result, _) <- withExecutionRecord hooks ExecutionCheckpoint start $ \\row -> do\n    (exit, rawOutput) <- runWasmWithInput limits program.wpModule program.wpInput dispatch\n    calls <- reverse <$> liftIO (readTVarIO receipts)\n    control <- liftIO (readTVarIO decisions)\n    overBudget <- liftIO (readTVarIO exhausted)\n    submittedCalls <- liftIO (readTVarIO submitted)\n    boundary <- liftIO (readTVarIO interrupted)\n    let parsed = traverse (eitherDecodeStrict' @Value) rawOutput\n        finalExit\n          | isJust (controlReply control) || isJust boundary = WasmHostStopped\n          | Left _ <- parsed = WasmTrapped \"guest output is not JSON\"\n          | exit == WasmCompleted,\n            Just contract <- program.wpOutputContract,\n            Left err <- validateValue contract (fromMaybe Null (fromRight Nothing parsed)) =\n              WasmTrapped (\"workflow output contract: \" <> err)\n          | otherwise = exit\n        result = CodeModeResult finalExit calls control (boundary <|> fromRight Nothing parsed) submittedCalls overBudget (maybe label ((\"journal#\" <>) . T.pack . show . (.jeJournalId)) row) program.wpWorkflow\n    pure (result, codeModeInvocation result)\n  pure result\n  where\n    digest = TE.decodeUtf8 . Base16.encode . SHA256.hash\n\nagentName, phaseName :: Text\nagentName = \"host:workflow_agent/v1\"\nphaseName = \"host:workflow_phase/v1\"\n\n-- Internal callback metadata is not added to the model's tool catalog. Only\n-- the host's agent batch may overlap queue-and-join callbacks; direct tool\n-- writes keep the existing SequentialOnly policy.\nhostCatalog :: [CatalogTool]\nhostCatalog =\n  [ CatalogTool (ToolDefinition (ToolRef name) (SchemaVersion 1) (Set.singleton (EffectWrite \"task\")) SequentialOnly RetryIdempotent (Set.singleton CurrentConversation) (ToolDeadline 21600) False mode) \"workflow host primitive\" (object []) (SchemaHash name)\n  | (name, mode) <- [(agentName, WorkCall), (phaseName, CheckpointCall)]\n  ]\n\nsteeringOutcome :: ToolOutcome -> Bool\nsteeringOutcome (ToolRejected fault) = fault.tfCode == \"workflow_steering_pending\"\nsteeringOutcome (ToolCommitted (Object fields)) = KeyMap.lookup \"interrupted\" fields == Just (Bool True) && KeyMap.lookup \"reason\" fields == Just (String \"workflow_steering_pending\")\nsteeringOutcome _ = False\n\ncodeModeInvocation :: CodeModeResult -> ToolInvocation\ncodeModeInvocation result = ToolInvocation outcome result.cmControl\n  where\n    count = length result.cmCalls\n    summary =\n      object\n        [ \"run_ref\" .= result.cmRunRef,\n          \"workflow\" .= result.cmWorkflow,\n          \"exit\" .= T.pack (show result.cmExit),\n          \"value\" .= result.cmOutput,\n          \"over_budget\" .= result.cmOverBudget,\n          \"call_count\" .= count,\n          \"submitted_calls\" .= result.cmSubmittedCalls,\n          \"omitted_calls\" .= max 0 (count - 128),\n          \"calls\" .= [object [\"call\" .= c.ccLabel, \"tool\" .= c.ccTool, \"outcome\" .= c.ccOutcome] | c <- drop (max 0 (count - 128)) result.cmCalls]\n        ]\n    -- Correction is safe only if every submitted call returned a known\n    -- pre-effect outcome (including no submissions). Missing receipts may hide\n    -- an interrupted effect, so compare counts before inspecting outcomes.\n    beforeEffects = count == result.cmSubmittedCalls && all ((`elem` [\"rejected\", \"failed-before-effect\"]) . (.ccOutcome)) result.cmCalls\n    outcome = case result.cmExit of\n      WasmCompleted -> ToolSucceeded summary\n      WasmHostStopped -> ToolSucceeded summary\n      _ | beforeEffects -> ToolFailedBeforeEffect (ToolFault \"wasm_failed_before_effect\" (TE.decodeUtf8 (wire summary)) RetrySafe)\n      _ -> ToolOutcomeUnknown (ToolFault \"wasm_interrupted\" (TE.decodeUtf8 (wire summary)) RetryUnsafe)\n\nwire :: Value -> ByteString\nwire = LBS.toStrict . encode\n\ndata GuestRequest = Invoke !Bool ![(Text, Value)] | ReadResult !Text !Int\n\n-- No task IDs, call IDs, schemas or controls can be supplied through the ABI.\nparseRequest :: ByteString -> Either Text GuestRequest\nparseRequest bytes = case eitherDecodeStrict' bytes of\n  Right value@(Object fields)\n    | Just single <- parseCall value -> Right (Invoke False [single])\n    | KeyMap.size fields == 1,\n      Just (Array calls) <- KeyMap.lookup \"calls\" fields,\n      not (null calls),\n      length calls <= 32,\n      Just parsed <- traverse parseCall (toList calls) ->\n        Right (Invoke True parsed)\n    | KeyMap.size fields == 2,\n      Just (String ref) <- KeyMap.lookup \"result_ref\" fields,\n      Just offset <- KeyMap.lookup \"offset\" fields >>= parseMaybe parseJSON ->\n        Right (ReadResult ref offset)\n  _ -> invalid\n  where\n    invalid = Left \"expected {tool: string, args: object}, {calls: [1..32 requests]}, or {result_ref: string, offset: integer}\"\n    parseCall (Object fields)\n      | KeyMap.size fields == 1, Just args@(Object _) <- KeyMap.lookup \"agent\" fields = Just (agentName, args)\n      | KeyMap.size fields == 1, Just (String label) <- KeyMap.lookup \"phase\" fields = Just (phaseName, String label)\n      | KeyMap.size fields == 2,\n        Just (String name) <- KeyMap.lookup \"tool\" fields,\n        not (T.null name),\n        name `notElem` [agentName, phaseName],\n        T.length name <= 256,\n        Just args@(Object _) <- KeyMap.lookup \"args\" fields =\n          Just (name, args)\n    parseCall _ = Nothing\n","src/Max/DB/ConversationLock.hs":"-- | Common commit-lock order for conversation-owned state. Callers hold a\n-- pinned transaction; these operations never start or commit one themselves.\nmodule Max.DB.ConversationLock (lockConversation, lockTurnConversation, lockTaskConversation) where\n\nimport Data.Int (Int64)\nimport Database.PostgreSQL.Simple.Types (Only (..))\nimport Effectful\nimport Effectful.PostgreSQL (WithConnection, query)\nimport Max.Turn.Types (AgentTurnId)\n\nlockTurnConversation :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Eff es Bool\nlockTurnConversation turn = do\n  rows <- query \"SELECT c.conversation_id FROM conversations c JOIN agent_turns t USING(conversation_id) WHERE t.turn_id=? FOR UPDATE OF c\" (Only turn)\n  pure (not (null (rows :: [Only Int64])))\n\nlockTaskConversation :: (WithConnection :> es, IOE :> es) => Int64 -> Eff es Bool\nlockTaskConversation task = do\n  rows <- query \"SELECT c.conversation_id FROM conversations c JOIN durable_tasks t USING(conversation_id) WHERE t.task_id=? FOR UPDATE OF c\" (Only task)\n  pure (not (null (rows :: [Only Int64])))\n\nlockConversation :: (WithConnection :> es, IOE :> es) => Int64 -> Eff es Bool\nlockConversation group = do\n  rows <- query \"SELECT conversation_id FROM conversations WHERE legacy_group_id=? FOR UPDATE\" (Only group)\n  pure (not (null (rows :: [Only Int64])))\n","src/Max/DB/Task/Admission.hs":"-- | Task admission binds provenance, authority and the request obligation in\n-- one transaction. PostgreSQL owns uniqueness; Haskell owns the policy.\nmodule Max.DB.Task.Admission (AdmissionError (..), admitTaskWithin) where\n\nimport Control.Monad (void, when)\nimport Data.Aeson (Value, encode)\nimport Data.ByteString.Lazy qualified as LBS\nimport Data.Int (Int64)\nimport Data.Map.Strict (Map)\nimport Data.Map.Strict qualified as Map\nimport Data.Maybe (fromMaybe, isJust, isNothing)\nimport Data.Text (Text)\nimport Data.Text qualified as T\nimport Data.Time (addUTCTime)\nimport Database.PostgreSQL.Simple.Types (Only (..))\nimport Effectful\nimport Effectful.PostgreSQL (WithConnection, execute, query)\nimport Max.DB.Task.Authorization\nimport Max.DB.Task.FrontendInput (closeInputWithin, unseenInputWithin)\nimport Max.DB.Task.Record\nimport Max.Task.Admission (AdmissionError (..))\nimport Max.Task.Policy (taskDeadlineSeconds)\nimport Max.Task.Types (TaskProfile, profileName, taskGrants)\nimport Max.Turn.Types (AgentTurnId)\n\nadmitTaskWithin :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Maybe Int64 -> Int64 -> Text -> Text -> TaskProfile -> Value -> Map Text Text -> Eff es (Either AdmissionError TaskRecord)\nadmitTaskWithin turn message actor key objective profile inputs grants = do\n  authorized <- authorizeWithin turn (ExecutionWork CheckOnly)\n  pending <- unseenInputWithin turn\n  sources <- query \"SELECT conversation_id,initiator_principal_id FROM agent_turns WHERE turn_id=?\" (Only turn)\n  case sources :: [(Int64, Maybe Int64)] of\n    [(_, Just _)] | authorized && pending -> pure (Left AdmissionInputPending)\n    [(conversation, Just initiator)] | authorized -> do\n      repeated <-\n        query\n          \"SELECT work.task_id FROM durable_tasks work WHERE work.admission_key=? AND (work.source_turn_id=?\\\n          \\ OR EXISTS(SELECT 1 FROM task_attempts execution WHERE execution.turn_id=? AND execution.task_id=work.parent_task_id AND execution.revision=work.parent_revision))\"\n          (key, turn, turn)\n      -- Check the authenticated actor even on idempotent replay; an opaque\n      -- admission key is not permission to discover another owner's task.\n      if initiator /= actor\n        then pure (Left InvalidAdmission)\n        else case repeated of\n          [Only identifier] -> requiredTask identifier\n          [] -> do\n            provenance <- case message of\n              Nothing -> pure True\n              Just source -> do\n                rows <- query \"SELECT EXISTS(SELECT 1 FROM messages WHERE canonical_message_id=? AND conversation_id=?)\" (source, conversation)\n                pure (rows == [Only True])\n            attempt <- loadAttempt turn\n            parent <- maybe (pure Nothing) (loadTask . (.taskId)) attempt\n            depth <- case parent of\n              Nothing -> pure 0\n              Just task -> do\n                rows <-\n                  query\n                    \"WITH RECURSIVE ancestors AS (SELECT task_id,parent_task_id FROM durable_tasks WHERE task_id=?\\\n                    \\ UNION ALL SELECT work.task_id,work.parent_task_id FROM durable_tasks work JOIN ancestors ON work.task_id=ancestors.parent_task_id) SELECT count(*) FROM ancestors\"\n                    (Only task.taskId)\n                case rows of [Only count] -> pure (count :: Int64); _ -> error \"ancestor aggregate missing\"\n            counts <- query \"SELECT count(*) FROM durable_tasks WHERE conversation_id=? AND status IN ('queued','running','waiting','retrying')\" (Only conversation)\n            let validInput = T.length key >= 1 && T.length key <= 600 && not (T.null (T.strip objective)) && T.length (T.strip objective) <= 40000 && LBS.length (encode inputs) <= 160000\n                narrower task = Map.isSubmapOfBy (==) grants task.grants\n            if not validInput\n              then pure (Left InvalidAdmission)\n              else\n                if not provenance\n                  then pure (Left AdmissionSourceOutsideScope)\n                  else\n                    if isJust attempt && isNothing parent\n                      then pure (Left AdmissionFenced)\n                      else\n                        if grants /= taskGrants profile grants || not (maybe True narrower parent)\n                          then pure (Left AdmissionWidenedAuthority)\n                          else\n                            if depth > 15\n                              then pure (Left AdmissionDepthLimit)\n                              else\n                                if any (\\(Only count) -> (count :: Int64) >= 160) counts\n                                  then pure (Left AdmissionQueueFull)\n                                  else do\n                                    now <- databaseNow\n                                    let admissionDeadline = addUTCTime (fromIntegral taskDeadlineSeconds) now\n                                        deadline = min admissionDeadline (maybe admissionDeadline (.deadline) parent)\n                                        parentId = (.taskId) <$> parent\n                                        rootId = (\\task -> fromMaybe task.taskId task.root) <$> parent\n                                    inserted <-\n                                      query\n                                        \"INSERT INTO durable_tasks(conversation_id,owner_principal_id,source_turn_id,source_message_id,admission_key,\\\n                                        \\ parent_task_id,parent_revision,root_task_id,objective,profile,inputs,grants,deadline,max_calls,max_rounds,created_at)\\\n                                        \\ VALUES(?,?,?,?,?,?,?,?,?,?,?::jsonb,?::jsonb,?,?,?,?) RETURNING task_id\"\n                                        ( conversation,\n                                          actor,\n                                          turn,\n                                          message,\n                                          key,\n                                          parentId,\n                                          (.revision) <$> parent,\n                                          rootId,\n                                          T.strip objective,\n                                          profileName profile,\n                                          jsonText inputs,\n                                          jsonText grants,\n                                          deadline,\n                                          200 :: Int,\n                                          400 :: Int,\n                                          now\n                                        )\n                                    case inserted of\n                                      [Only identifier] -> do\n                                        when (isNothing parent) (closeInputWithin turn)\n                                        void $ execute \"INSERT INTO task_revisions(task_id,revision,objective,author_principal_id) VALUES(?,1,?,?)\" (identifier, T.strip objective, actor)\n                                        when (isJust message && isNothing parent) $\n                                          void $\n                                            execute\n                                              \"INSERT INTO conversation_requests(message_id,turn_id,disposition) VALUES(?,?,'delegated')\\\n                                              \\ ON CONFLICT(message_id) DO UPDATE SET disposition='delegated',updated_at=now()\"\n                                              (message, turn)\n                                        requiredTask identifier\n                                      _ -> error \"task admission did not create a task\"\n          _ -> error \"task admission key is not unique\"\n    _ -> pure (Left AdmissionFenced)\n  where\n    requiredTask identifier = loadTask identifier >>= maybe (error \"admitted task missing\") (pure . Right)\n","src/Max/DB/Task/Authorization.hs":"-- | Execution authority is checked under the conversation commit lock. This\n-- module never calls an effectful tool; it reserves budgets before that edge.\nmodule Max.DB.Task.Authorization\n  ( StepReservation (..),\n    ExecutionStep (..),\n    authorizeWithin,\n    authorizeCallerWithin,\n    reserveResourceWithin,\n  )\nwhere\n\nimport Control.Monad (void)\nimport Data.Int (Int64)\nimport Data.Maybe (fromMaybe)\nimport Data.Text (Text)\nimport Database.PostgreSQL.Simple.Types (Only (..))\nimport Effectful\nimport Effectful.PostgreSQL (WithConnection, execute, query)\nimport Max.DB.Task.Record\nimport Max.Execution.Types (ExecutionStep (..), StepReservation (..))\nimport Max.Platform.Types (PrincipalId (..))\nimport Max.Task.State\nimport Max.Turn.Types (AgentTurnId)\nimport OneBot.Types (GroupId (..))\n\n-- | The caller owns a pinned transaction. Re-read turn state after acquiring\n-- its conversation lock; a pre-lock snapshot is never sufficient authority.\nauthorizeWithin :: (WithConnection :> es, IOE :> es) => AgentTurnId -> ExecutionStep -> Eff es Bool\nauthorizeWithin turn step = do\n  locked <- lockTurnConversation turn\n  active <-\n    if locked\n      then\n        query\n          \"SELECT frontend_managed FROM agent_turns WHERE turn_id=? AND status IN ('starting','running','recovery-pending') FOR UPDATE\"\n          (Only turn)\n      else pure []\n  case active of\n    [Only managed] -> do\n      frontend <- query \"SELECT EXISTS(SELECT 1 FROM conversation_frontends WHERE turn_id=? AND lease_until>clock_timestamp())\" (Only turn)\n      stale <-\n        query\n          \"SELECT EXISTS(SELECT 1 FROM task_notifications notice JOIN durable_tasks work USING(task_id) LEFT JOIN task_progress progress USING(task_id)\\\n          \\ WHERE notice.turn_id=? AND (notice.revision<>work.revision OR notice.attempt<>work.attempt\\\n          \\ OR notice.body->>'status' IS DISTINCT FROM work.status OR work.status='cancelled' OR notice.superseded_at IS NOT NULL\\\n          \\ OR notice.review_decision->>'action'='skip'\\\n          \\ OR (notice.kind='progress' AND notice.progress_version IS DISTINCT FROM progress.version)))\"\n          (Only turn)\n      if (managed && frontend /= [Only True]) || stale == [Only True]\n        then pure False\n        else do\n          attempt <- loadAttempt turn\n          case attempt of\n            Nothing -> pure True\n            Just execution -> do\n              work <- loadTask execution.taskId\n              case work of\n                Nothing -> pure False\n                Just task -> do\n                  root <- loadTask (fromMaybe task.taskId task.root)\n                  ancestors <-\n                    query\n                      \"WITH RECURSIVE ancestors AS (SELECT parent_task_id,parent_revision FROM durable_tasks WHERE task_id=?\\\n                      \\ UNION ALL SELECT work.parent_task_id,work.parent_revision FROM durable_tasks work JOIN ancestors ON work.task_id=ancestors.parent_task_id)\\\n                      \\ SELECT EXISTS(SELECT 1 FROM ancestors JOIN durable_tasks work ON work.task_id=ancestors.parent_task_id\\\n                      \\ WHERE work.status NOT IN ('running','queued','waiting','retrying') OR work.revision<>ancestors.parent_revision)\"\n                      (Only task.taskId)\n                  now <- databaseNow\n                  case root of\n                    Just budget\n                      | task.status == Running\n                          && task.revision == execution.revision\n                          && task.attempt == execution.attempt\n                          && execution.leaseUntil > now\n                          && taskIsLive budget.status\n                          && ancestors == [Only False] ->\n                          case step of\n                            ExecutionCheckpoint -> pure True\n                            ExecutionWork reservation\n                              | task.deadline <= now || budget.deadline <= now -> pure False\n                              | not (available reservation task && available reservation budget) -> pure False\n                              | otherwise -> do\n                                  case reservation of\n                                    CheckOnly -> pure ()\n                                    ReserveCall -> void $ execute \"UPDATE durable_tasks SET calls_reserved=calls_reserved+1 WHERE task_id IN (?,?)\" (task.taskId, budget.taskId)\n                                    ReserveRound -> void $ execute \"UPDATE durable_tasks SET rounds_reserved=rounds_reserved+1 WHERE task_id IN (?,?)\" (task.taskId, budget.taskId)\n                                  pure True\n                    _ -> pure False\n    _ -> pure False\n  where\n    available CheckOnly _ = True\n    available ReserveCall task = task.calls < task.maxCalls\n    available ReserveRound task = task.rounds < task.maxRounds\n\n-- | Sandbox affinity is a scoped reservation, retained for unknown effects\n-- after terminal work. The resource row never grants execution authority.\nreserveResourceWithin :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Text -> Eff es Bool\nreserveResourceWithin turn resource = do\n  authorized <- authorizeWithin turn ExecutionCheckpoint\n  present <-\n    query\n      \"SELECT EXISTS(SELECT 1 FROM sandboxes sandbox JOIN agent_turns turn USING(conversation_id)\\\n      \\ WHERE turn.turn_id=? AND sandbox.sandbox_handle=? AND sandbox.status<>'destroyed')\"\n      (turn, resource)\n  if not authorized || present /= [Only True]\n    then pure False\n    else do\n      attempt <- loadAttempt turn\n      (_ :: [Only Int]) <-\n        query\n          \"SELECT 1::integer FROM (SELECT pg_advisory_xact_lock(hashtextextended('task-resource:'||?::text,0))) reservation\"\n          (Only resource)\n      void $\n        execute\n          \"DELETE FROM task_resource_owners owner USING durable_tasks work WHERE owner.task_id=work.task_id\\\n          \\ AND owner.resource=? AND work.status NOT IN ('running','queued','waiting','retrying')\\\n          \\ AND NOT EXISTS(SELECT 1 FROM task_attempts execution JOIN execution_journal journal USING(turn_id)\\\n          \\ WHERE execution.task_id=owner.task_id AND journal.state IN ('started','outcome-unknown') AND journal.normalized_input->>'sandbox_id'=?)\"\n          (resource, resource)\n      owners <- query \"SELECT task_id FROM task_resource_owners WHERE resource=?\" (Only resource)\n      let identifier = (.taskId) <$> attempt\n      case owners :: [Only Int64] of\n        [Only current] -> pure (Just current == identifier)\n        [] -> do\n          case identifier of\n            Just task -> void $ execute \"INSERT INTO task_resource_owners(resource,task_id) VALUES(?,?)\" (resource, task)\n            Nothing -> pure ()\n          pure True\n        _ -> error \"resource uniqueness violated\"\n\n-- | Reuse the locked execution check without accepting a caller-selected actor\n-- or conversation. Callers keep this inside the mutation's pinned transaction.\nauthorizeCallerWithin :: (WithConnection :> es, IOE :> es) => AgentTurnId -> GroupId -> PrincipalId -> Eff es Bool\nauthorizeCallerWithin turn (GroupId group) (PrincipalId actor) = do\n  authorized <- authorizeWithin turn (ExecutionWork CheckOnly)\n  identity <- query \"SELECT EXISTS(SELECT 1 FROM agent_turns JOIN conversations USING(conversation_id) WHERE turn_id=? AND initiator_principal_id=? AND legacy_group_id=?)\" (turn, actor, group)\n  pure (authorized && identity == [Only True])\n","src/Max/DB/Task/Record.hs":"-- | Facts read under the conversation lock. Domain transitions live in\n-- Haskell; this module owns row decoding and the common lock order.\nmodule Max.DB.Task.Record\n  ( TaskRecord (..),\n    AttemptRecord (..),\n    loadTask,\n    loadAttempt,\n    lockTurnConversation,\n    lockTaskConversation,\n    lockConversation,\n    databaseNow,\n    jsonText,\n  )\nwhere\n\nimport Data.Aeson (ToJSON, Value, encode)\nimport Data.ByteString.Lazy qualified as LBS\nimport Data.Int (Int64)\nimport Data.Map.Strict (Map)\nimport Data.Text (Text)\nimport Data.Text.Encoding qualified as TE\nimport Data.Time (UTCTime)\nimport Database.PostgreSQL.Simple.FromRow (FromRow (..), field)\nimport Database.PostgreSQL.Simple.Types (Only (..))\nimport Effectful\nimport Effectful.PostgreSQL (WithConnection, query)\nimport Max.DB.Codec (enumField, jsonField)\nimport Max.DB.ConversationLock (lockConversation, lockTaskConversation, lockTurnConversation)\nimport Max.Task.State\nimport Max.Task.Types (TaskProfile, parseProfile)\nimport Max.Turn.Types (AgentTurnId)\n\ndata TaskRecord = TaskRecord\n  { taskId :: !Int64,\n    conversationId :: !Int64,\n    owner :: !Int64,\n    sourceTurn :: !AgentTurnId,\n    sourceMessage :: !(Maybe Int64),\n    parent :: !(Maybe Int64),\n    parentRevision :: !(Maybe Int),\n    root :: !(Maybe Int64),\n    monitorFire :: !(Maybe Int64),\n    revision :: !Int,\n    objective :: !Text,\n    profile :: !TaskProfile,\n    inputs :: !Value,\n    grants :: !(Map Text Text),\n    status :: !TaskStatus,\n    result :: !(Maybe TaskReport),\n    -- Reserved usage, including work still in flight; see loadTask's projection.\n    calls :: !Int,\n    rounds :: !Int,\n    maxCalls :: !Int,\n    maxRounds :: !Int,\n    attempt :: !Int,\n    consumedEvent :: !Int64,\n    deadline :: !UTCTime,\n    retryCount :: !Int\n  }\n  deriving stock (Show, Eq)\n\ninstance FromRow TaskRecord where\n  fromRow =\n    TaskRecord\n      <$> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> enumField parseProfile\n      <*> jsonField\n      <*> jsonField\n      <*> enumField parseTaskStatus\n      <*> jsonField\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n      <*> field\n\n-- | The caller holds the conversation lock before locking work. Parent,\n-- child, frontend and notification transitions therefore share one order.\nloadTask :: (WithConnection :> es, IOE :> es) => Int64 -> Eff es (Maybe TaskRecord)\nloadTask identifier = do\n  rows <-\n    query\n      \"SELECT task_id,conversation_id,owner_principal_id,source_turn_id,source_message_id,\\\n      \\ parent_task_id,parent_revision,root_task_id,monitor_fire_id,revision,objective,profile,\\\n      \\ inputs::text,grants::text,status,COALESCE(result,'null'::jsonb)::text,\\\n      \\ calls_reserved,rounds_reserved,max_calls,max_rounds,attempt,consumed_event,deadline,retry_count\\\n      \\ FROM durable_tasks WHERE task_id=? FOR UPDATE\"\n      (Only identifier)\n  pure $ case rows of [row] -> Just row; _ -> Nothing\n\ndata AttemptRecord = AttemptRecord\n  { taskId :: !Int64,\n    revision :: !Int,\n    attempt :: !Int,\n    owner :: !Text,\n    leaseUntil :: !UTCTime,\n    seenEvent :: !Int64,\n    report :: !(Maybe TaskReport),\n    retryable :: !Bool\n  }\n  deriving stock (Show, Eq)\n\ninstance FromRow AttemptRecord where\n  fromRow = AttemptRecord <$> field <*> field <*> field <*> field <*> field <*> field <*> jsonField <*> field\n\nloadAttempt :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Eff es (Maybe AttemptRecord)\nloadAttempt turn = do\n  rows <-\n    query\n      \"SELECT task_id,revision,attempt,owner,lease_until,seen_event,COALESCE(report,'null'::jsonb)::text,retryable FROM task_attempts WHERE turn_id=? FOR UPDATE\"\n      (Only turn)\n  pure $ case rows of [row] -> Just row; _ -> Nothing\n\ndatabaseNow :: (WithConnection :> es, IOE :> es) => Eff es UTCTime\ndatabaseNow = do\n  rows <- query \"SELECT clock_timestamp()\" ()\n  case rows of [Only now] -> pure now; _ -> error \"database clock missing\"\n\njsonText :: (ToJSON a) => a -> Text\njsonText = TE.decodeUtf8 . LBS.toStrict . encode\n","src/Max/DB/Task/Scheduling.hs":"-- | Bounded scheduling under one reservation lock. SQL selects eligible\n-- rows; Haskell owns limits, deadlines, retries and explicit settlement.\nmodule Max.DB.Task.Scheduling (claimTask) where\n\nimport Control.Monad (forM_, void, when)\nimport Data.Int (Int64)\nimport Data.Text (Text)\nimport Data.Time (addUTCTime)\nimport Database.PostgreSQL.Simple.Types (Only (..))\nimport Effectful\nimport Effectful.PostgreSQL (WithConnection, execute, query)\nimport Max.DB.Task.Record\nimport Max.DB.Task.Settlement\nimport Max.DB.Transaction (withTransaction)\nimport Max.Task.State\nimport Max.Turn.Types (AgentTurnId)\n\nclaimTask :: (WithConnection :> es, IOE :> es) => Text -> Eff es [AgentTurnId]\nclaimTask owner = withTransaction $ do\n  -- Shared with all claimers, so capacity checks and reservation commit once.\n  locked <- query \"SELECT 1::integer FROM (SELECT pg_advisory_xact_lock(872008)) reservation\" ()\n  case locked :: [Only Int] of\n    [_] -> pure ()\n    _ -> error \"task scheduling lock missing\"\n  candidates <- eligibleTask Nothing\n  case candidates of\n    [Only identifier] -> do\n      _ <- lockTaskConversation identifier\n      -- Control/settlement could have changed the candidate while this worker\n      -- was waiting for its conversation. Re-evaluate eligibility under lock.\n      stillEligible <- eligibleTask (Just identifier)\n      task <- if stillEligible == candidates then loadTask identifier else pure Nothing\n      maybe (pure []) start task\n    _ -> pure []\n  where\n    start task = do\n      now <- databaseNow\n      if task.deadline <= now || task.attempt >= 40\n        then do\n          completeTask task BudgetExhausted (Just (TaskReport ReportBudgetExhausted \"deadline or retry budget exhausted\" [] [] Nothing Nothing Nothing))\n          pure []\n        else do\n          when (task.status == Running) $ do\n            -- Fence old attempts before settling their turns. Their reports\n            -- cannot overwrite the next attempt or replay unknown effects.\n            void $ execute \"UPDATE durable_tasks SET status='queued' WHERE task_id=?\" (Only task.taskId)\n            void $\n              execute\n                \"UPDATE execution_journal SET state='outcome-unknown',finished_at=now(),failure_code='task_lease_expired'\\\n                \\ WHERE state='started' AND turn_id IN (SELECT turn_id FROM task_attempts WHERE task_id=?)\"\n                (Only task.taskId)\n            expired <-\n              query\n                \"UPDATE agent_turns SET status='crashed',finished_at=now(),abort_reason='task execution lease expired'\\\n                \\ WHERE status IN ('starting','running','recovery-pending') AND turn_id IN (SELECT turn_id FROM task_attempts WHERE task_id=?) RETURNING turn_id,frontend_managed\"\n                (Only task.taskId)\n            forM_ expired $ \\(turn, managed) -> settleTurn turn SettlementFailed (Just \"task execution lease expired\") managed\n          turns <-\n            query\n              \"INSERT INTO agent_turns(conversation_id,turn_ordinal,trigger_canonical_message_id,initiator_principal_id,status)\\\n              \\ SELECT ?,COALESCE(max(turn_ordinal),0)+1,?,?,'starting' FROM agent_turns WHERE conversation_id=? RETURNING turn_id\"\n              (task.conversationId, task.sourceMessage, task.owner, task.conversationId)\n          case turns of\n            [Only turn] -> do\n              void $ execute \"UPDATE durable_tasks SET status='running',attempt=attempt+1,next_attempt_at=NULL,updated_at=now() WHERE task_id=?\" (Only task.taskId)\n              void $\n                execute\n                  \"INSERT INTO task_attempts(turn_id,task_id,revision,attempt,owner,lease_until) VALUES(?,?,?,?,?,?)\"\n                  (turn, task.taskId, task.revision, task.attempt + 1, owner, addUTCTime 60 now)\n              pure [turn]\n            _ -> error \"task claim did not create a turn\"\n\neligibleTask :: (WithConnection :> es, IOE :> es) => Maybe Int64 -> Eff es [Only Int64]\neligibleTask identifier =\n  query\n    \"SELECT work.task_id FROM durable_tasks work WHERE (work.status='queued'\\\n    \\ OR work.status='retrying' AND work.next_attempt_at<=clock_timestamp()\\\n    \\ OR work.status='running' AND NOT EXISTS (SELECT 1 FROM task_attempts execution WHERE execution.task_id=work.task_id\\\n    \\ AND execution.attempt=work.attempt AND execution.lease_until>clock_timestamp())\\\n    \\ OR work.status IN ('waiting','retrying') AND work.deadline<=clock_timestamp())\\\n    \\ AND (SELECT count(*) FROM durable_tasks active JOIN task_attempts execution ON execution.task_id=active.task_id AND execution.attempt=active.attempt\\\n    \\ WHERE active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits waiting WHERE waiting.turn_id=execution.turn_id))<?\\\n    \\ AND (SELECT count(*) FROM durable_tasks active JOIN task_attempts execution ON execution.task_id=active.task_id AND execution.attempt=active.attempt\\\n    \\ WHERE active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits waiting WHERE waiting.turn_id=execution.turn_id) AND active.conversation_id=work.conversation_id)<?\\\n    \\ AND (SELECT count(*) FROM durable_tasks active JOIN task_attempts execution ON execution.task_id=active.task_id AND execution.attempt=active.attempt\\\n    \\ WHERE active.status='running' AND execution.lease_until>clock_timestamp() AND NOT EXISTS(SELECT 1 FROM workflow_agent_waits waiting WHERE waiting.turn_id=execution.turn_id) AND active.owner_principal_id=work.owner_principal_id)<?\\\n    \\ AND (work.monitor_fire_id IS NULL OR NOT EXISTS (\\\n    \\ SELECT 1 FROM durable_tasks active JOIN monitor_fires active_fire ON active_fire.fire_id=active.monitor_fire_id\\\n    \\ JOIN monitor_fires candidate_fire ON candidate_fire.fire_id=work.monitor_fire_id\\\n    \\ WHERE active.task_id<>work.task_id AND active.status='running' AND active_fire.monitor_id=candidate_fire.monitor_id\\\n    \\ AND EXISTS(SELECT 1 FROM task_attempts execution WHERE execution.task_id=active.task_id AND execution.attempt=active.attempt AND execution.lease_until>clock_timestamp())))\\\n    \\ AND (work.parent_task_id IS NULL OR EXISTS (SELECT 1 FROM durable_tasks parent WHERE parent.task_id=work.parent_task_id AND parent.status IN ('queued','running','waiting','retrying')))\\\n    \\ AND (?::bigint IS NULL OR work.task_id=?)\\\n    \\ ORDER BY (SELECT max(previous.updated_at) FROM durable_tasks previous WHERE previous.owner_principal_id=work.owner_principal_id AND previous.attempt>0) NULLS FIRST,\\\n    \\ work.updated_at,work.task_id LIMIT 1\"\n    (40 :: Int, 10 :: Int, 10 :: Int, identifier, identifier)\n","src/Max/DB/Task/Workflow.hs":"-- | Durable workflow joins over existing tasks. Admission, fencing and budgets\n-- remain owned by the task subsystem; no model or guest runs in a transaction.\nmodule Max.DB.Task.Workflow\n  ( AgentStep (..),\n    beginAgentStep,\n    pollAgentStep,\n    endAgentWait,\n    workflowAllowed,\n    workflowSteeringPending,\n  )\nwhere\n\nimport Control.Monad (void, when)\nimport Data.Aeson (Value, object, (.=))\nimport Data.Int (Int64)\nimport Data.Map.Strict (Map)\nimport Data.Map.Strict qualified as Map\nimport Data.Maybe (isNothing)\nimport Data.Text (Text)\nimport Database.PostgreSQL.Simple.Types (Only (..))\nimport Effectful\nimport Effectful.PostgreSQL (WithConnection, execute, query)\nimport Max.DB.Task.Admission (admitTaskWithin)\nimport Max.DB.Task.Authorization\nimport Max.DB.Task.Record\nimport Max.DB.Transaction (withTransaction)\nimport Max.Task.Admission (admissionErrorText)\nimport Max.Task.Delegation\nimport Max.Task.State\nimport Max.Task.Types\nimport Max.Turn.Types (AgentTurnId)\n\ndata AgentStep = AgentStep\n  { childId :: !Int64,\n    childRevision :: !Int,\n    originalJournal :: !Int64,\n    cached :: !(Maybe Value)\n  }\n  deriving stock (Eq, Show)\n\n-- Frontends may still run ordinary workflows. An awaited child may only run\n-- the normal agent loop, including after a crash/restart of that child.\nworkflowAllowed :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Eff es Bool\nworkflowAllowed turn = do\n  rows <- query \"SELECT NOT EXISTS(SELECT 1 FROM task_attempts attempt JOIN workflow_agent_steps step ON step.child_task_id=attempt.task_id WHERE attempt.turn_id=?)\" (Only turn)\n  pure (rows == [Only True])\n\nworkflowSteeringPending :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Eff es Bool\nworkflowSteeringPending turn = do\n  rows <-\n    query\n      \"SELECT EXISTS(SELECT 1 FROM task_events event JOIN durable_tasks work USING(task_id) JOIN task_attempts attempt USING(task_id) \\\n      \\ WHERE attempt.turn_id=? AND attempt.revision=work.revision AND attempt.attempt=work.attempt \\\n      \\ AND event.kind='steer' AND event.event_id>GREATEST(work.consumed_event,attempt.seen_event))\"\n      (Only turn)\n  pure (rows == [Only True])\n\nbeginAgentStep :: (WithConnection :> es, IOE :> es) => AgentTurnId -> Map Text Text -> Value -> AgentRequest -> Int64 -> Eff es (Either Text AgentStep)\nbeginAgentStep turn currentGrants receipts request journal = withTransaction $ do\n  allowed <- authorizeWithin turn (ExecutionWork CheckOnly)\n  pending <- workflowSteeringPending turn\n  attempt <- loadAttempt turn\n  parent <- maybe (pure Nothing) (loadTask . (.taskId)) attempt\n  case parent of\n    Just task | allowed && not pending && isNothing task.parent && Map.member \"task_start\" currentGrants && Map.lookup \"task_start\" currentGrants == Map.lookup \"task_start\" task.grants -> do\n      let same name contract = Map.lookup name task.grants == Just contract\n          authority = Map.filterWithKey same currentGrants\n          grants = taskGrants request.profile authority\n          required = case request.profile of Research -> Nothing; Browser -> Just \"browser_navigate\"; Sandbox -> Just \"sandbox_exec\"; Operations -> Just \"maxops_execute\"\n          key = agentCallKey (object [\"receipts\" .= receipts, \"grants\" .= grants]) request\n      if maybe False (\\name -> not (Map.member name grants)) required\n        then pure (Left \"requested profile exceeds the parent capability ceiling\")\n        else do\n          previous <-\n            query\n              \"SELECT child_task_id,child_revision,COALESCE(settled_journal_id,first_journal_id),result FROM workflow_agent_steps WHERE parent_task_id=? AND parent_revision=? AND call_key=?\"\n              (task.taskId, task.revision, key)\n          case previous of\n            [(child, revision, original, result)] -> do\n              work <- loadTask child\n              case work of\n                Just existing | existing.revision == revision && existing.status /= Cancelled -> do\n                  when (isNothing result && existing.status `elem` [Queued, Running, Retrying]) (markWait child)\n                  pure (Right (AgentStep child revision original result))\n                _ -> pure (Left \"cached child generation was invalidated; use a changed objective/input after inspecting its effects\")\n            [] -> do\n              let inputs = object [\"context\" .= request.inputs, \"output_contract\" .= request.outputContract, \"workflow_agent\" .= True]\n              admitted <- admitTaskWithin turn Nothing task.owner key request.objective request.profile inputs grants\n              case admitted of\n                Left failure -> pure (Left (admissionErrorText failure))\n                Right child -> do\n                  void $\n                    execute\n                      \"INSERT INTO workflow_agent_steps(parent_task_id,parent_revision,call_key,child_task_id,child_revision,first_journal_id) VALUES(?,?,?,?,?,?)\"\n                      (task.taskId, task.revision, key, child.taskId, child.revision, journal)\n                  markWait child.taskId\n                  pure (Right (AgentStep child.taskId child.revision journal Nothing))\n            _ -> error \"workflow step identity is not unique\"\n    _ -> pure (Left (if pending then \"workflow_steering_pending\" else \"agent() requires a live root task with delegation authority\"))\n  where\n    markWait child = void $ execute \"INSERT INTO workflow_agent_waits(turn_id,child_task_id) VALUES(?,?) ON CONFLICT DO NOTHING\" (turn, child)\n\n-- Nothing means the child is still executing; a returned report is data.\n-- Read and settle under the same parent-generation lock as cancellation.\npollAgentStep :: (WithConnection :> es, IOE :> es) => AgentTurnId -> AgentStep -> Maybe Value -> Int64 -> Eff es (Either Text (Maybe Value))\npollAgentStep turn step contract journal = withTransaction $ do\n  allowed <- authorizeWithin turn (ExecutionWork CheckOnly)\n  child <- if allowed then loadTask step.childId else pure Nothing\n  case child of\n    Just task | task.revision == step.childRevision ->\n      case task.result of\n        Just report | task.status `notElem` [Queued, Running, Retrying] -> do\n          let result = agentReport task.status report contract\n          if taskIsLive task.status || task.status == Cancelled\n            then pure ()\n            else\n              void $\n                execute\n                  \"UPDATE workflow_agent_steps SET result=?,settled_journal_id=? WHERE child_task_id=? AND child_revision=? AND result IS NULL\"\n                  (result, journal, step.childId, step.childRevision)\n          pure (Right (Just result))\n        _ -> pure (Right Nothing)\n    _ -> pure (Left \"parent or child execution was cancelled, superseded or expired\")\n\nendAgentWait :: (WithConnection :> es, IOE :> es) => AgentTurnId -> AgentStep -> Eff es ()\nendAgentWait turn step = void $ execute \"DELETE FROM workflow_agent_waits WHERE turn_id=? AND child_task_id=?\" (turn, step.childId)\n","src/Max/Task/Delegation.hs":"-- | Closed request and normalized report for awaited workflow children.\n-- These values describe requested work; they never grant authority.\nmodule Max.Task.Delegation\n  ( AgentRequest (..),\n    parseAgentRequest,\n    agentCallKey,\n    agentReport,\n    validateAgentPayload,\n  )\nwhere\n\nimport Control.Monad (unless)\nimport Data.Aeson\nimport Data.Aeson.KeyMap qualified as KM\nimport Data.Aeson.Types (parseEither)\nimport Data.ByteString.Lazy qualified as LBS\nimport Data.Foldable (traverse_)\nimport Data.Text (Text)\nimport Data.Text qualified as T\nimport Max.Skill.Contract (validateContract, validateValue)\nimport Max.Task.Experience (fingerprint)\nimport Max.Task.State\nimport Max.Task.Types\n\ndata AgentRequest = AgentRequest\n  { objective :: !Text,\n    inputs :: !Value,\n    profile :: !TaskProfile,\n    outputContract :: !(Maybe Value)\n  }\n  deriving stock (Eq, Show)\n\ninstance ToJSON AgentRequest where\n  toJSON request = object [\"objective\" .= request.objective, \"inputs\" .= request.inputs, \"profile\" .= profileName request.profile, \"output_contract\" .= request.outputContract]\n\nparseAgentRequest :: Value -> Either Text AgentRequest\nparseAgentRequest raw = do\n  request <-\n    either (Left . T.pack) Right $\n      parseEither\n        ( withObject \"agent request\" $ \\o -> do\n            unless (all (`elem` [\"objective\", \"inputs\", \"profile\", \"output_contract\"]) (KM.keys o)) (fail \"unknown agent argument\")\n            name <- o .: \"profile\"\n            profile <- maybe (fail \"unknown capability profile\") pure (parseProfile name)\n            AgentRequest . T.strip <$> o .: \"objective\" <*> o .:? \"inputs\" .!= Null <*> pure profile <*> o .:? \"output_contract\"\n        )\n        raw\n  unless (not (T.null request.objective) && T.length request.objective <= 40000 && LBS.length (encode raw) <= 65536) (Left \"agent objective/input exceeds its bound\")\n  traverse_ validateContract request.outputContract\n  pure request\n\n-- Source edits create new journal steps. Unchanged semantic calls can still\n-- reuse their child in the same parent revision. Loaded receipts are part of\n-- the call identity, so a package update invalidates that reuse.\nagentCallKey :: Value -> AgentRequest -> Text\nagentCallKey receipts request = \"agent:\" <> fingerprint (object [\"receipts\" .= receipts, \"request\" .= request])\n\nvalidateAgentPayload :: Maybe Value -> TaskReport -> Either Text ()\nvalidateAgentPayload Nothing _ = Right ()\nvalidateAgentPayload (Just contract) report\n  | report.status == ReportSucceeded = maybe (Left \"succeeded requires payload for the requested output_contract\") (validateValue contract) report.payload\n  | otherwise = traverse_ (validateValue contract) report.payload\n\nagentReport :: TaskStatus -> TaskReport -> Maybe Value -> Value\nagentReport status report contract =\n  object\n    [ \"status\" .= status,\n      \"findings\" .= report.summary,\n      \"evidence\" .= report.evidence,\n      \"unresolved\" .= report.unresolved,\n      \"payload\" .= (if validateAgentPayload contract report == Right () then report.payload else Nothing),\n      \"payload_valid\" .= either (const False) (const True) (validateAgentPayload contract report)\n    ]\n","src/Max/Task/Types.hs":"module Max.Task.Types\n  ( TaskProfile (..),\n    profileName,\n    parseProfile,\n    taskGrants,\n    taskHandle,\n    parseTaskHandle,\n  )\nwhere\n\nimport Data.Int (Int64)\nimport Data.Map.Strict (Map)\nimport Data.Map.Strict qualified as Map\nimport Data.Text (Text)\nimport Data.Text qualified as T\nimport Text.Read (readMaybe)\n\ndata TaskProfile = Research | Browser | Sandbox | Operations\n  deriving stock (Eq, Show)\n\nprofileName :: TaskProfile -> Text\nprofileName Research = \"research\"\nprofileName Browser = \"browser\"\nprofileName Sandbox = \"sandbox\"\nprofileName Operations = \"operations\"\n\nparseProfile :: Text -> Maybe TaskProfile\nparseProfile \"research\" = Just Research\nparseProfile \"browser\" = Just Browser\nparseProfile \"sandbox\" = Just Sandbox\nparseProfile \"operations\" = Just Operations\nparseProfile _ = Nothing\n\ntaskGrants :: TaskProfile -> Map Text Text -> Map Text Text\ntaskGrants profile = Map.filterWithKey (\\name _ -> name `elem` allowed)\n  where\n    allowed =\n      [ \"web_search\",\n        \"maxops_operations\",\n        \"maxops_query\",\n        \"get_message_by_id\",\n        \"context_search\",\n        \"context_expand\",\n        \"view_forward\",\n        \"memory_list\",\n        \"view_image\",\n        \"view_video\",\n        \"view_avatar\",\n        \"view_bilibili\",\n        \"use_skill\",\n        \"task_start\",\n        \"task_status\",\n        \"task_list\",\n        \"task_steer\"\n      ]\n        <> case profile of\n          Research -> []\n          Operations -> [\"maxops_execute\"]\n          Browser ->\n            [ \"browser_navigate\",\n              \"browser_snapshot\",\n              \"browser_click\",\n              \"browser_type\",\n              \"browser_scroll\",\n              \"browser_press_key\",\n              \"browser_screenshot\",\n              \"browser_back\",\n              \"browser_wait_for\",\n              \"view_zhihu\"\n            ]\n          Sandbox ->\n            [ \"sandbox_list\",\n              \"sandbox_create\",\n              \"sandbox_destroy\",\n              \"sandbox_exec\",\n              \"sandbox_read_file\",\n              \"sandbox_write_file\",\n              \"import_file_to_sandbox\",\n              \"nix_search\"\n            ]\n\ntaskHandle :: Int64 -> Text\ntaskHandle identifier = \"task#\" <> T.pack (show identifier)\n\nparseTaskHandle :: Text -> Maybe Int64\nparseTaskHandle raw = do\n  digits <- T.stripPrefix \"task#\" (T.strip raw)\n  if T.null digits || T.any (\\character -> character < '0' || character > '9') digits\n    then Nothing\n    else do\n      identifier <- readMaybe (T.unpack digits)\n      if identifier > 0 then Just identifier else Nothing\n","src/Max/Task/WorkflowRuntime.hs":"-- | Bind workflow host operations to the current task authority. Polling holds\n-- no database connection or transaction while the ordinary worker runs a child.\nmodule Max.Task.WorkflowRuntime (taskWorkflowHost) where\n\nimport Control.Concurrent (threadDelay)\nimport Data.Aeson\nimport Data.Aeson.KeyMap qualified as KM\nimport Data.Map.Strict qualified as Map\nimport Data.Text (Text)\nimport Data.Text qualified as T\nimport Effectful\nimport Effectful.Exception (finally, mask, throwIO)\nimport Effectful.PostgreSQL (WithConnection)\nimport Max.CodeMode.JavaScript (javaScriptRuntimeVersion)\nimport Max.DB.Task.Reporting (submitProgress)\nimport Max.DB.Task.Workflow\nimport Max.Execution.Types (JournalExecution (..))\nimport Max.Execution.Workflow\nimport Max.Task.Delegation\nimport Max.Tasks (TaskCancelled (..))\nimport Max.Tool.Bundles (SkillLoad (..))\nimport Max.Tool.Control (LoopControl (ContinueLoop))\nimport Max.Tool.Types\nimport Max.ToolContext\nimport Max.Turn.Types (AgentTurnRef (..))\n\ntaskWorkflowHost :: (WithConnection :> es, IOE :> es) => ToolContext -> AgentTurnRef -> WorkflowHost es\ntaskWorkflowHost context turn =\n  WorkflowHost\n    { whAllowed = workflowAllowed turn.atrTurnId,\n      whAgent = runAgent,\n      whPhase = \\label -> do\n        accepted <- submitProgress turn.atrTurnId label\n        pure $ if accepted then committed (object [\"recorded\" .= True]) else rejected \"workflow_phase_rejected\" \"phase requires a current task execution\",\n      whParallel = True\n    }\n  where\n    receipts = object [\"runtime\" .= javaScriptRuntimeVersion, \"skills\" .= Map.map (.slVersion) (toolSkillLoads context)]\n    runAgent raw journal = case (parseAgentRequest raw, journal) of\n      (Left detail, _) -> pure (rejected \"invalid_agent_request\" detail)\n      (_, Nothing) -> pure (rejected \"agent_requires_durable_task\" \"agent() requires a durable root task\")\n      (Right request, Just entry) -> mask $ \\restore -> do\n        started <- beginAgentStep turn.atrTurnId (toolCatalogGrants context) receipts request entry.jeJournalId\n        case started of\n          Left detail -> pure (rejected (if detail == \"workflow_steering_pending\" then detail else \"agent_admission_rejected\") detail)\n          Right step -> restore (await entry request step) `finally` endAgentWait turn.atrTurnId step\n    await entry request step = case step.cached of\n      Just report -> pure (committed (receipt step True report))\n      Nothing -> do\n        report <- pollAgentStep turn.atrTurnId step request.outputContract entry.jeJournalId\n        case report of\n          Left _ -> throwIO TaskCancelled\n          Right (Just value) -> pure (committed (receipt step False value))\n          Right Nothing -> do\n            pending <- workflowSteeringPending turn.atrTurnId\n            if pending\n              then pure (committed (receipt step False (object [\"interrupted\" .= True, \"reason\" .= (\"workflow_steering_pending\" :: Text)])))\n              else liftIO (threadDelay 250000) >> await entry request step\n    receipt step reused (Object fields) = Object (KM.union fields (KM.fromList [\"task\" .= (\"task#\" <> fromStringId step.childId), \"reused\" .= reused, \"original_execution\" .= (\"journal#\" <> fromStringId step.originalJournal)]))\n    receipt _ _ value = value\n    fromStringId = T.pack . show\n    committed value = ToolInvocation (ToolCommitted value) ContinueLoop\n    rejected code detail = ToolInvocation (ToolRejected (ToolFault code detail RetrySafe)) ContinueLoop\n"}}